---
title: 'Inbox Evolution Roadmap — Arc 5: the kept-promises arc'
status: 'CLOSED 2026-07-30 by arc-closeout — do not append. This arc is archived — do not append. Shipped v0.9.0 2026-07-30 (builds B5-1..B5-9; commit f3b343d, tag v0.9.0, pushed; B5-4..B5-9 as one owner-ruled batch commit 43795fa). Acceptance: discharged 2026-07-30 by acceptance-discharge off the vlt-core 0.8.0->0.9.0 own-path upgrade (ledger [2026-07-30 13:20]) + the filed vlt-sayari work-vault instrument run — B5-1 ticked in full, B5-2..B5-9 ship-verifiable clause (1) all discharged with evidence, zero FAILED/BLOCKED; all open clause-(2) tails field-contingent, released to the standing watch register in the Closeout record (released, not ticked — the [x] count is not what the arc proved). Still open elsewhere: the A4-4(5) Jackson-pair inherited debt (second arc, B5-2 tail, carrier 160949 active); the B5-3..B5-9 first-exercise watches (first full/scoped lint, spec promote/decline + the two-unstamped-specs backfill ruling, next vlt-sayari upgrade, activation/consult/dispatch spot-checks + owner say-so notes, first lint persist, the 091003 M0 audit, first strip/ledger-glance); the pre-Arc-5 carries (vlt-track loop-profile, BMB drift upstream, lifecycle-audit Items 4+7, sayari lint-render + non-answer-consult watches). Authoritative register: this roadmap''s Closeout record section. Filings: 5 archived (082933, 111133, 183003, and owner-ruled no-build 171500 + 141500), 14 held active against the watches.'
module_code: 'vlt'
created: '2026-07-29'
updated: '2026-07-30 (ACCEPTANCE-DISCHARGE — vlt-core 0.9.0 upgrade evidence walked onto the ledger: B5-1 DISCHARGED in full (baseline artifact + the filed vlt-sayari work-vault run; both filings archived), B5-2..B5-9 upgrade-side clause (1) all DISCHARGED with grep/run/hash evidence incl. a live vlt-vitals run with 3 figures hand-verified exact; zero FAILED/BLOCKED; still-open tails all field-contingent + non-gating; next: arc-closeout. Prior: RELEASE — v0.9.0 shipped via vlt-release: arc5-v0.9.0 branch; B5-4..B5-9 landed as one owner-ruled batch build commit 43795fa (entangled shared files made per-build commits unreconstructable — advisory mismatch recorded); handshake exit 0 (9 conventions, 29 pins bipartite-consistent); dual bump marketplace.json + module.yaml → 0.9.0; package-lint: A/B/C/E PASS, D PASS — vlt 0.9.0, exit 0, PASS line verbatim in the release commit f3b343d; ff-only merge to main, tagged v0.9.0, main + tag pushed to github.com/mggower/bmad-module-vlt with owner confirmation; roadmap SHIPPED-stamped; next: owner runs vlt-upgrade on vlt-core, then acceptance-discharge. Prior: BUILD — B5-9 built (implemented directly; the brief governed): F1–F10 + registration landed — NEW skills/vlt-setup/assets/hooks/vlt-vitals.py (stdlib-only; the shared measurement substrate re-homed from cost-manifest.py, which now imports it — one parser, two callers; the canonical 10-id METRICS table; registry evaluation with absent-registry-as-plain-zero and loud unknown-metric errors; full-report + --strip modes, silent-when-green, vitals-unavailable-on-failure exit 1); NEW assets/tripwires.yaml (schema-in-header; lint-debt ingests_since_lint ≥ 10 + relay-overdue oldest_open_pointer_days > 21, no third wire); module.yaml default map + the contract''s semantic table gain tripwires + lint_reports (E2 held); vlt-setup §2b (hook force-reinstall to {root}/.claude/hooks/, seed merge-by-id with local-wins, idempotent settings.json SessionStart registration keyed on the bare script path never touching vault-local hooks, {lint_reports} created) with vlt-upgrade verified-not-edited (Step-6 reconfigure hand-off carries §2b); the contract''s {log} section named a declared machine-read grammar (parsers case-insensitive + paren-tolerant; paren mandatory going forward, history grandfathered) with the rule-card SHA re-stamped in step (stale C6 FAIL observed first); vlt-lint description names lint-debt as its counter, Step-0 two-definitions pin, Step-6 reset-by-derivation + the persist beat ({lint_reports}/YYYY-MM-DD-HHMM-lint.md, both modes, append-only), checks.md''s two promises now live pointers, counter_unknown_metric in the meta-check + report fence (strict-YAML whole PASS); ledger.md Tripped-wires & vitals (denominated zero, warning-row-on-failure, display-only vitals block, blocked-facet grouping) + Verify line, router blurb +3 words; frontmatter 4→5 (blocked: user-decision | partner-bandwidth | external-event YYYY-MM-DD, optional, external-event requires its date, zero backfill; :231 one-vocabulary reword) with the SIX-consumer walk — five re-acks + vlt-dispatch joining consumers: and acking frontmatter@5 first-time, daily/relay paren notes; the non-convention-deferral wire rule at vlt-mint Step 2a + council Step 3; package-lint C8 (seed parses + wires complete, metric ids from the imported table, py_compile to a temp path, both map rows). Verified at rest: package-lint A/B/C/E PASS with C8 live + three temp-copy negatives FAIL correctly; Group E bipartite frontmatter@5 ↔ six consumers; test-cost-manifest 7/7; fixture vitals hand-verified both modes (green silence / unavailable exit-1 / loud wire errors); settings-merge demo idempotent with the vault-local hook byte-identical; single-home greps clean; scrub clean; no cruft; no commit. 4 numbered deviations in the brief''s status; next: the arc RELEASE via vlt-release (B5-4..B5-9 UNCOMMITTED; dual bump + --expect-version gate). Prior: BRIEF — B5-9 briefed by build-brief; pinned pre-brief re-read (091003 M0 + parked Arc-3 rulings) discharged BEFORE scoping; re-ground clean against HEAD 2f19251 + B5-4..B5-8''s uncommitted edits, zero grounding corrections (pre-recut lint/dispatch addresses re-home per the documented B5-8 re-cut), five grounding additions; designated questions ruled: A5-18 → dated artifact at new `lint_reports: _agent/lint-reports/` map path (both modes, append-only; session-note home rejected), days_since_lint → display-only, blocked_on → inline greppable facet w/ external-event companion date, riding frontmatter 4→5 + six-consumer walk (vlt-dispatch joins); A5-7 shape 3 → derived denominated display-only streak line (shape 2 cited NOT-BLOCKING); wire forms reconciled (frontmatter deferrals per-convention, registry vault-level, one finder per fact); substrate re-homed to shipped asset assets/hooks/vlt-vitals.py with cost-manifest importing (the :40 open note resolves); Arc-3 decide-once rulings cited (hooks home, find-vs-nag, hardcoded dispatch read); POST-HOC DEFAULT cited; F1–F10 + C8; two acceptance checks in the ledger; next: build B5-9, then the arc release. Prior: BUILD — B5-8 built (implemented directly; the brief governed): F1–F5 + no-registration landed. F1 dispatch re-cut per mode — router SKILL.md 11,281 bytes (≤ 14,000) keeping the frontmatter whole (acks byte-identical), Overview, On Activation with the act-blocking rules verbatim, Mode dispatch naming each mode''s reference as the read-on-entry (every mode reads its reference on entry, ledger included), the pickup loop in full, and a per-mode Log preamble; NEW references/daily.md 11,007 / relay.md 7,031 / consult.md 9,126 / ledger.md 1,811, each carrying its mode body verbatim + its {log} line shape + its Verify checklist; total 40,256 ≤ 40,785 (105% of 38,843). F2 lint re-cut per step — router 6,261 bytes (≤ 12,000) keeping frontmatter (8 acks byte-identical), Overview, On Activation incl. the JIT-read rule, Step 0 scope with the early exit router-only + the full-scale.md pointer, Step 1, the six-line step-sequence map, the act-blocking Standing rules (never-auto-fix, the never-auto-apply list, single-writer-in-the-SKILL, never-stamp-adoption, write-through-records-only), and Step 6; NEW references/full-scale.md 3,611 / checks.md 25,023 / fix-and-file.md 6,243 / report.md 10,306 with the Step-5 fence strict-YAML-parsing whole at its new home; total 51,444 ≤ 51,476 (105% of 49,025). F3 package-lint C7 router-integrity (orphan reference / dangling route both FAIL; ROUTER_BUDGETS beside RULE_CARD_BUDGET, homed in Group C) + E3 widened to skills/vlt-*/references/*.md (not a blanket glob). F4 ack-coverage clause widened to reference files, landed in references/checks.md riding the move — no convention edited. F5 cost-manifest whale aggregates dir-aware (surface + router rows for both whales), skill-surface note added, harness truth table follows. Verified at rest: package-lint A/B/C/E PASS and three temp-copy negatives each FAIL correctly (deleted reference → dangling route; planted frontmatter@4 in a reference → widened E3; stray unrouted reference → orphan); test-cost-manifest 7/7; both frontmatter blocks byte-identical pre/post and zero convention files touched — the no-bump machine-confirmed; single-home greps clean both directions; inbound-pointer walk resolves through surviving mode/step names with ZERO inbound edits; vlt-upgrade:48-49 verified-not-edited (the wholesale copy carries references/, the exclusion list does not catch the name, skill_asset_divergence accommodates new shipped files); dry-walks complete incl. the nothing-in-scope lint exit reading the router alone; scrub clean; no .decision-log.md; no commit — release rides the arc level. No deviations in the brief''s status; next: brief B5-9 last (B5-4..B5-8 UNCOMMITTED awaiting release). Prior: BRIEF — B5-8 briefed by build-brief; re-ground clean against HEAD 2f19251 + B5-4..B5-7''s uncommitted working-tree edits (every A5-14 site HOLDS, whales strengthened — lint 49,025 / dispatch 38,843 bytes; zero grounding corrections; four grounding additions: cost-manifest whale aggregates, package-lint E3 scope, the vlt-upgrade reports/-exclusion naming constraint, vlt-lint:82 ack coverage). The pinned package-lint.py packaging-expectations check performed BEFORE scoping (six findings in the brief — E3 widening + a new deterministic router-integrity check ride the build). Under POST-HOC DEFAULT: (a) adopted for both skill whales — dispatch per-mode (references/daily|relay|consult|ledger.md; router keeps identity/routing/act-blocking + the pickup loop; ~45–60% per single-mode invocation), lint per-step (references/full-scale|checks|fix-and-file|report.md; honest bound: full runs conserve, savings in scoped/early/trigger paths); (c) discharged by B5-7, no contract byte; (d) satisfied; (b) inside the move. Budgets: dispatch router ≤14,000, lint ≤12,000, per-skill total ≤105% pre-cut. No convention bump; frontmatter.md declined with reasons. Two acceptance checks in the ledger; next: build B5-8, then brief B5-9 last. Prior: BUILD — B5-7 built (implemented directly; the brief governed): F1–F8 + registration landed — NEW _meta/vault-rule-card.md (5,511 bytes; derived_from: sha256 of the shipped contract + derivation date; derived-artifact marker with the honest-limit line; becoming frame; six act-blocking rule rows, each a pointer at its contract section; the 19-row H2 section map); the three partner openers re-pointed at the card byte-identically with the "read is reinforcement" concession dropped, and the contract''s ritual section (:161) moved in step; Beat-2 bounds in both homes ({index} headings-tier open-on-demand, {log} last-5, {backlog} open items, ## Thread only with ## Set aside excluded; dispatch slice + capabilities unchanged) plus the contract-home rationale clause carrying the measured figures; the already-fresh skip widened to the rule-card read at both homes (Beat 1 outside); the consult-lite boot — one exemption sentence in the engine consultPrompt (no ritual, no rule-card/contract read, no Beat-2 orient; depth-1 traffic, not a sitting), the mechanics sentence at dispatch §consult (entry lite, exit already exempt), the descriptive sentence in consult.md §What-a-consult-is with version: 1 kept and no re-ack (Group E the check of record); cost-manifest module mode recomposed (fixed boot = SKILL+rule-card; the contract wholly in lazy governance stock, broken-out table + named-aggregate row kept; vault-mode last-5 slice relabeled the declared Beat-2 bound); package-lint gains C6 derived-artifact freshness (card exists, derived_from sha256 equals the shipped contract''s actual hash, ≤8,000 bytes) homed inside Group C so the release summary format holds; vlt-setup:144 copy list carries the card with the module-owned overwrite posture stated; vlt-upgrade:48 verified carrying _meta/ wholesale, no edit needed. Verified at rest: package-lint A/B/C/E PASS and a one-byte contract perturbation in a temp copy FAILs C6; test-cost-manifest 7/7; node --check clean; fixed boot 15,896–17,324 bytes vs the baseline''s 48,340–49,768 rows (~64–67% cut, ≥60% the bound); two-home greps uniform; scrub clean; no .decision-log.md; no commit. 4 numbered deviations/notes in the brief''s status (contract intro :17 also re-pointed; F8 homed as C6; harness slice-label assertion updated; upgrade verified-not-edited); next: brief B5-8, then B5-9 last. Prior: BUILD — B5-6 built via bmad-workflow-builder: F1–F6 landed as briefed (NEW {conventions}/decision-log.md at version 1 — entry schema with the required ref: machine key, two-tier classifiability tail (no kind: = pre-schema unclassifiable; kind: without ref: = scopable-by-class, unaddressable-by-subject; no backfill, surfaced never swept), supersession idiom stated as the third application of the never-silent principle citing wiki-supersession.md (Q21 discharged: one principle, three bespoke applications; the vlt-mint:84 debt clause retired), writers/readers roster, honest limit, enforcement checked/vlt-lint/lint-run with a derived-never-stored counter and no deferral block, adoption_first_instance null; vlt-setup:144 brace list gains decision-log; vlt-mint:68-84 re-cut to the pointer section with the schema fence + mechanics deleted and the ack added; the seed template shrunk to a pointer, worked example dropped per the brief''s cheaper cut; vlt-lint gains the ack, the read-before-flag preface over the long-lived governance findings deriving adjudicated/undisposed/unclassifiable with the recorded-only bound stated and never-auto-fix untouched, the Step-3 write-through beat that never stamps adoption, the governance_memory:/rulings_recorded: report keys and their compose-yourself paragraph; vlt-upgrade gains the ack, the reconcile pass''s §5 second scan leg — overlays/base divergences/retired capabilities with no live ref:-keyed entry surface once for ruling, idempotent, with the two-tier cannot-classify honesty bound — and the :85 shape pointer re-targeted at the convention). Verified at rest per the brief: package-lint A/B/C/E PASS with Group E bipartite positively confirmed (decision-log v1 ↔ all three acks at decision-log@1); single-home + Q21-retirement greps clean; whole-fence yaml.safe_load PASS with both new keys and the convention frontmatter strict-YAML flat-key clean; temp-fixture walk PASS (adjudication cites the live superseding entry, the superseded twin is no match target, 1 pre-schema + 1 pre-key counted unclassifiable, the undisposed overlay surfaces exactly once and skips on re-run); rsync demo confirmed the dotted .decision-log.md exclusion does not catch the undotted convention basename; durability re-read clean (the new leg only surfaces; Step 3.7 writes, human-gated); scrub clean; no .decision-log.md cruft; no commit. 3 numbered deviations in the brief''s status (fence placeholder de-bracketed for strict YAML; step anchors added to the F3 pointer; template example dropped not updated); next: brief B5-7 — noting B5-7/B5-8 are gated on the B5-1 measurement numbers (the work-vault run is outstanding, owner''s) and the owner''s deferred pre-hoc/post-hoc governance ruling, with B5-9 last. Prior: BRIEF — B5-6 briefed by build-brief; re-ground clean against HEAD 2f19251 + B5-4''s and B5-5''s uncommitted working-tree edits (the grounding baseline — B5-4 touched vlt-lint/vlt-mint/vlt-upgrade/the governance bundle, B5-5 vlt-upgrade further): every A5-4 capture site HOLDS (the schema home vlt-mint:68-84 with the fence :74-80 and the Q21 debt clause :84; vlt-lint decision-log grep-zero with the long-lived governance findings shifted :81-84→:82-87; vlt-upgrade :77 reconcile + :85 write-through; contract honest-reporting :254 with the extended proxy-check rule :260 and the derive-first boundary clause :262); zero grounding corrections; four grounding additions inside the ruling''s letter (vlt-setup:144''s enumerated copy list per the A4-5 F2 precedent; the seed template decision-log-template.md as a second schema restatement to shrink to a pointer; B5-4''s stamp authority rule bounding the new lint write-through — lint never stamps; the vlt-upgrade:49 dotted-.decision-log.md copy-exclusion confirmed not matching the undotted convention basename). The promotion ruling (b) implemented as briefed: NEW {conventions}/decision-log.md at version 1 (entry schema + required ref: machine key + two-tier classifiability tail + supersession idiom, enforcement checked/vlt-lint/lint-run, adoption null) with consumers [vlt-mint, vlt-upgrade, vlt-lint] all acking decision-log@1 in the same build (Group E the check of record); vlt-mint :68-84 and the seed template re-cut to pointers; vlt-lint gains read-before-flag (adjudicated/undisposed/unclassifiable) + the lint-time-ruling write-through + governance_memory:/rulings_recorded: keys; vlt-upgrade :77 reconcile extended with the filing''s §5 leg (overlays/base divergences/retired capabilities with no live ref:-keyed entry surface once, idempotent) and :85 re-pointed. Five brief-time dispositions recorded, notably Q21 DISCHARGED as one-principle-three-applications (the never-silent principle stays single-homed in wiki-supersession.md, cited by decision-log.md; no merged super-convention; the vlt-mint:84 debt clause retires) and vlt-setup ruled a non-consumer. Two acceptance checks in the ledger; next: build B5-6. Prior: BUILD — B5-5 built via bmad-workflow-builder: F1–F6 landed in vlt-upgrade/SKILL.md as briefed (Step-1 bullet retitled **Local mints** with the provenance test — per-kind shipped definition, both-kinds bracket-path fallback — plus the Overview both-kinds phrase; Step-2 own-the-apply prohibition widened to unshipped local-mint vlt-* dirs; Step 3.1 confirm + Step 3.2 restore widened to local mints; mints_preserved/bodies_restored placeholders and the ledger Mints-preserved comment broadened with key names kept per disposition 1; Verify widened to local-mint dirs and extended with the nine-line ledger field-completeness check per disposition 2; the one-sentence required-lines annotation after the Step-5 template); vlt-mint:168 and merge-help-csv.py deliberately unedited per the out-of-scope dispositions. Verified at rest per the brief: widening grep clean (survivors genuinely partner-specific), Verify list ↔ Step-5 template one-to-one, dry-read coherent, B1 durability fixture run PASS (local_mints_preserved lists both the partner and the op skill, zombie dropped), vlt-mint:168 promise confirmed true under the widened Step 1, package-lint A/B/C/E PASS, scrub clean; 3 numbered deviations in the brief''s status (the Overview :17 prohibition restatement widened as a seventh name-scoped surface; frontmatter description left unchanged; no commit — release choreography rides the arc level); next: brief B5-6. Prior: BRIEF — B5-5 briefed by build-brief; re-ground clean against HEAD 2f19251 + B5-4''s uncommitted working-tree edits (the grounding baseline, since B5-4 touched vlt-upgrade extensively): every A5-2 site HOLDS (:34 name-scoped bullet, :35-40 no op-skill bullet, :48 prohibition, :66 B2, :97-98/:124-125 report+ledger fields, vlt-mint:168 promise, merge-help-csv.py:192-230 provenance test) and every A5-17 site HOLDS (:105 key, :112 never-omit, :131 template line, :146 existence-only Verify); zero grounding corrections; two grounding additions: Verify''s own "minted-partner dir" clause (:146) is a sixth name-scoped site, and Step 3.1''s confirm clause (:64) under-claims the script''s provenance-based preserve. Designated questions ruled in the brief: A5-2''s mints_preserved ruled BROADEN (filing''s default adopted — one provenance class, one field; key name kept), A5-17 ruled Verify-side field-completeness check as the mechanism (re-read the appended block, confirm all nine template lines) + one-line template required-lines annotation as belt, and the report-side sub-question ruled NO (the ledger is the durable artifact); vlt-mint:168 deliberately unedited (becomes true in the same release, per filing §5). Two acceptance checks in the ledger; next: build B5-5. Prior: BUILD — B5-4 built via bmad-workflow-builder: F1–F7 landed as briefed (spec.md 1→2 with the *Promotion from candidate* beat — named-owner derivation, promote/decline terminal states, honored-decline exclusion — plus the deferral blind-spot statement, and all four consumers re-acked spec@2; vlt-lint''s spec-candidate check re-cut to the dated-revision-record signal with the named-owner routing pointer, the Step-4 item shape + decline guard, and the spec_candidate:/consult_retroactive: report keys; the consult precedence comparison added beside pair-presence with consult.md''s Enforcement prose sharpened to name both states (no bump); vlt-upgrade''s retrofit signal re-cut in step + the first-instance stamp beat; the stamp authority rule (ceremony-that-produces-the-first-instance) single-homed at vlt-mint Step 4 with frontmatter.md:242 made descriptive (no bump), vlt-dispatch''s consult-block stamp beat, the adoption_first_instance: exclusion in both divergence compares, and the apply carry-forward; the adoption null now reports declared-since age from created:); package-lint A/B/C/E PASS with Group E bipartite at spec@2, whole-fence yaml.safe_load PASS with the two new keys, two-site heading grep clean (the heading survives only inside not-revision-evidence clauses), temp-fixture divergence-exclusion walk PASS (stamp-only clean, genuine edit still flags); 2 numbered deviations in the brief''s status (report-key placeholders de-colonized per the brief''s own fence rule; the Step-4 report template line aged to match the :112 definition); next: brief B5-5. Prior: BRIEF — B5-4 briefed by build-brief; re-ground clean at 2f19251 (every A5-6/A5-9/A5-15 site HOLDS with B5-2/B5-3''s trivial vlt-lint shifts; zero grounding corrections; two grounding additions: the missing Step-5 `spec_candidate:` report key, and the upgrade half of the stamp lock-out — Step 2''s wholesale base refresh would clobber any stamp); designated questions ruled in the brief: A5-6''s three directions all adopted (the promotion step is the spec 1→2 rule change, four-consumer re-ack), A5-15 ruled second-writer via the ceremony-that-produces-the-first-instance authority rule with the stamp landing in base frontmatter + adoption-aware divergence compares + upgrade carry-forward, A5-9''s heading leg replaced by a dated-revision-record signal at both sites (git rejected) + the consult_retroactive third state (consult.md/frontmatter.md prose-only, no bump); two acceptance checks in the ledger; next: build B5-4. Prior: BUILD — B5-3 built via bmad-workflow-builder: F1–F8 landed as briefed (verbatim-extraction re-cut of PAGE_SCAN/INDEX_SCAN with JS-computed link normalization and summary/category/attestation verdicts; sources_count deleted; ≥4-page family-stem disqualifier; stubSlugs third exclusion channel; tier-1 sources-vs-prose check homed in write-verification@2 with all four consumers re-acked and the lint-side Step-2 producer added; strict-YAML fence fixed; extended proxy-check rule appended to the contract''s Honest reporting); positive 19-page fixture met every expectation (alias/anchor/prefix/.md links clean, registered stub excluded, the one genuine miss reported, computed verdicts exact incl. over-length (170 chars), family pairs gone, the true french-press stem pair kept); negative fixture clean with the return shape intact; package-lint A/B/C/E + whole-fence yaml.safe_load PASS; 4 numbered deviations in the brief''s status (two extra pre-existing fence breaks fixed; normalizeTarget strips wrapping brackets; title made required in PAGE_SCAN; normalizeTarget homed at args-intake); next: brief B5-4. Prior: BRIEF — B5-3 briefed by build-brief; re-ground clean at 2c3740b (B5-2 shifted lines in both target files ~+7/+4; every A5-8/A5-3 capture site HOLDS in substance; zero grounding corrections; one sharpening, not a correction: the workflow''s Gap-B key fills the slot in full-at-scale mode only — A5-3''s gap is the SKILL''s own Step 2, exactly as captured); the designated (a)/(b)/(c) slot question RULED (b) in the brief — define the check, tier-1 per the write-verification membership test, which makes it a convention rule change: write-verification 1→2 + four-consumer re-ack; brief also rules the sources_count deletion, the ≥4-pages family-stem disqualifier, the stubSlugs third exclusion channel, and writes the extended proxy-check rule into the contract''s Honest reporting section (cross-filing ruling 3, F8); two acceptance checks in the ledger; next: build B5-3. Prior: BUILD — B5-2 built via bmad-workflow-builder: F1-F5 landed as briefed (PAGE_SCAN name_callout_targets + seeded pair pass + entity_scan_facts in vlt-lint-full.js; second-leg paragraph, widened entity_scan: denominator + composition rule, Step 4 duplicate-filing guard in vlt-lint/SKILL.md); positive fixture reproduced the split-pair loss AND surfaced the pair (callout-seeded) with seeded_pairs_checked=1; negative fixture ran zero pair agents with seeded_pairs_total=0 and shape intact; package-lint A/B/C/E PASS; 2 numbered deviations in the brief status (pairCap zero-honoring guard; cap path verified by code inspection per the brief''s allowance); next: brief B5-3. Prior: BRIEF — B5-2 briefed by build-brief; re-ground clean at c1a4f9b (B5-1 touched tools/ only; every A5-16 capture site HOLDS — cluster formation trivially shifted :204-219→:206-220; vault-side callout confirmed at los-angeles-rams.md:76 with the :72 direct link, corroborating greedy consumption); zero grounding corrections; second-pass shape ruled in the brief (callout-seeded pair pass, cluster formation untouched); two checks appended to the ledger; next: build B5-2. Prior: BUILD — B5-1 built via bmad-workflow-builder: tools/cost-manifest.py two-mode instrument + test harness (7/7) + cost-baseline-2026-07-29.md gate artifact; vlt-core vault-mode run read-only-proven, 4 figures hand-verified; 4 numbered deviations in the brief status (no-fallbacks-on-vlt-core, bracket-swallowed-header tolerance, bytes labeling, last-5 slice bound); next: brief B5-2. Prior: BRIEF — B5-1 briefed by build-brief; re-ground clean at 9f05579, zero corrections; Deferred acceptance ledger section created with B5-1''s two checks; next: build B5-1. Prior: RE-SLUG — owner ruling: the seed batch was captured into Arc 4''s roadmap only because Arc 5 wasn''t scaffolded when the filings arrived post-ship; captures renumbered A5-1..A5-18 (+A5-19 transfer), and inbox-capture''s Discovery rule sharpened so ship day, not closeout day, is the capture boundary. Prior: CAPTURE — arc stood up; carry-forwards re-listed; build-17/091003 transfer grounded against v0.8.0 @ 9f05579)'
arc_title_status: 'PROPOSED — clerk-derived from the cross-filing thread (shipped surfaces promise checks, persistence, and enforcement that nothing yet pays for); owner may rename before the first brief lands.'
derives_from:
  # ── the Arc-5 seed batch: captured + grounded in the Arc-4 roadmap (archived), 2026-07-29 @ 9f05579.
  #    Grounded text lives THERE (single-home) — this roadmap points, never restates.
  - 'inbox/2026-07-25-171500-brief-restatement-drift.md'
  - 'inbox/2026-07-25-183003-upgrade-preserve-set-misses-vault-grown-op-skills.md'
  - 'inbox/2026-07-25-193000-report-slot-with-no-check.md'
  - 'inbox/2026-07-26-124223-lint-has-no-memory-of-adjudicated-divergence.md'
  - 'inbox/2026-07-26-141500-group-e-did-not-retire-the-handwritten-handshake-grep.md'
  - 'inbox/2026-07-26-142000-spec-convention-surfaces-candidates-nobody-accepts.md'
  - 'inbox/2026-07-26-142500-boundary-classifier-five-verdicts-and-an-unmeasurable-metric.md'
  - 'inbox/2026-07-26-184704-lint-full-asks-llms-for-exact-facts.md'
  - 'inbox/2026-07-26-184705-spec-candidate-revision-signal-is-template-boilerplate.md'
  - 'inbox/2026-07-29-082930-activation-contract-read-is-the-boot-whale.md'
  - 'inbox/2026-07-29-082931-handoffs-and-consults-repay-the-full-boot.md'
  - 'inbox/2026-07-29-082932-beat-2-orient-scales-with-vault-age.md'
  - 'inbox/2026-07-29-082933-no-instrument-measures-session-token-cost.md'
  - 'inbox/2026-07-29-082934-whale-files-carry-restated-weight.md'
  - 'inbox/2026-07-29-120001-adoption-stamp-unreachable-beyond-mint.md'
  - 'inbox/2026-07-29-120002-entity-collision-coverage-pair-incomplete.md'
  - 'inbox/2026-07-29-120003-sayari-upgrade-omitted-convention-adoption-line.md'
  - 'inbox/2026-07-29-120004-lint-report-block-is-never-persisted.md'
  # ── captured this run (2026-07-29): the Arc-3 → Arc-5 transfer ──
  - 'inbox/2026-07-06-091003-enforcement-kit-derive-first.md'
predecessor: 'skills/reports/archive/inbox-evolution-arc4-roadmap.md (Arc 4 — the honest-surface arc; CLOSED 2026-07-29, builds A4-1..A4-5 shipped v0.8.0, acceptance discharged over four runs; A4-4 clause (5) FAILED and owner-carried here as A5-16, archive id A4-21)'
intent: >
  Arc 4 made the module's surfaces honest; this arc's raw material reports what honest
  surfaces cost and what they still cannot do. Nineteen filings, four threads: the checks
  behind the checks (proxy checks, template-vocabulary signals, bells that never ring),
  writes with no surface (stamps, report blocks, and ledger lines nothing persists or
  verifies), the price of the machinery (the five-filing boot-cost anatomy, measured with
  numbers for the first time), and the enforcement kit (build-17) — the never-built
  substrate that shipped prose already names as the escalation home for three of the
  other threads' fixes.
---

# Arc 5 — the kept-promises arc

## The through-line (why these filings are one story)

Arc 4's closing capture already bent the story a quarter-turn: the seed batch "reports what
honest surfaces cost and what they still can't do." Adding the build-17 transfer closes the
circle, because the enforcement kit is the thing the other threads keep pointing at:

1. **The checks behind the checks** (A5-3, A5-6, A5-7, A5-8, A5-9) — a report key no
   check fills, a `spec_candidate` beat whose terminal state is the backlog, a classifier
   whose bell has never rung, a lint that asks LLM scanners for exactly-computable facts, a
   revision signal that detects the module's own template vocabulary. The same silent-zero
   anatomy Arc 4 fixed, one layer down.
2. **Writes with no surface** (A5-2, A5-4, A5-15, A5-17, A5-18) — an adoption stamp only
   mints may write, a divergence decision the lint can't remember, an upgrade-report line
   nothing verifies, a lint report nothing persists, a vault-grown op skill no preserve
   bullet catches.
3. **The price of the machinery** (A5-10..A5-14) — the boot-cost anatomy, which asks, for
   the first time with numbers, whether the governance the arcs have been stacking is paid
   for at every activation.
4. **The enforcement kit** (A5-19, build-17 / filing 091003) — vault state lives in prose, so
   nothing can count, trigger, or trip. Grounding this run found the kit has become a
   **dangling referent in shipped prose**: `vlt-lint` twice defers escalation to "a tripwire
   concern (the enforcement kit), not lint's" (`skills/vlt-lint/SKILL.md:62`, `:104`), and
   the enforcement-declaration schema holds an `enforcement_counter:` slot "optional until
   the enforcement kit lands" (`conventions/frontmatter.md:231`). Three releases of shipped
   surface now promise a mechanism that was never built.

The factory itself appears twice (A5-1, A5-5): the loop's own briefs and templates drift
the same way vault surfaces do.

The proposed arc name reads the four threads as one sentence: the module's surfaces make
promises — a check will fill this slot, a counter will trip this wire, this cost is worth
paying — **and this arc is where the module either keeps them or stops making them.**
Ideation may rename.

## The seed batch (A5-1..A5-18, re-slugged from the archive's A4-6..A4-23)

Captured and grounded in the **Arc-4 roadmap** (`skills/reports/archive/
inbox-evolution-arc4-roadmap.md`, *Capture — 18 filings (grounded against module source
2026-07-29, v0.8.0 @ `9f05579`)*) under **A4-6..A4-23 ids**, because Arc 4's roadmap was
the only open one when the filings arrived — three days *after* Arc 4 shipped.

**Owner ruling (2026-07-29): re-slug.** These captures belong to Arc 5 and carry A5-* ids;
the A4-* numbering was an artifact of a Discovery rule that treated awaiting-acceptance as
open-for-capture (rule fixed the same day in `inbox-capture`'s Discovery — ship day, not
closeout day, is the capture boundary). The archive is read-only and keeps its A4-* ids;
**the table below is the permanent mapping** — an A4-* id in the archive, the filings, or
any pre-ruling record resolves through it. All Arc-5 artifacts from here on (ideation
rulings, briefs, ledger) use the A5-* ids only. ~~Build ids assigned at ideation continue the
arc's number space (A5-20+, per the Arc-4 precedent of one shared per-arc space) so capture
and build ids never collide.~~ **AMENDED at ideation (owner ruling, 2026-07-29): builds carry
their own prefix and count from 1 — `B5-1`, `B5-2`, … — as each arc's build count did before
(A4-1..A4-5 began at 1).** The Arc-4 "one shared space" reading mistook why no collision
happened there: Arc 4's captures carried A3-* ids, so its builds could start at 1 without
clashing. Here captures own A5-1..A5-19, so builds take the `B5-*` shape instead; an A5-* id
is always a capture, a B5-* id always a build.

The full grounded text (verified `file:line` citations, corrections, honest limits,
per-filing cross-references, open design questions) lives in the archive and is **not
restated here** — it remains the single home of that grounding. HEAD is still `9f05579` at
this capture, so the grounding is current as written. Digest below is for navigation only.

| Id | Grounded as | Filing | Thread | Verdict at capture (digest) |
|---|---|---|---|---|
| A5-1 | A4-6 | brief restatement drift | factory | shipped half SUPERSEDED (build A4-1 fixed it); factory half GAP CONFIRMED |
| A5-2 | A4-7 | upgrade preserve set misses vault-grown op skills | writes-no-surface | GAP CONFIRMED in full; intact at HEAD |
| A5-3 | A4-8 | report slot with no check | checks-behind-checks | GAP CONFIRMED, still true at HEAD |
| A5-4 | A4-9 | lint has no memory of adjudicated divergence | writes-no-surface | GAP CONFIRMED in full; no A4 build touched the seam |
| A5-5 | A4-10 | Group E did not retire the handwritten handshake grep | factory | items 1–2 FIXED (do not re-brief); item 3 GAP |
| A5-6 | A4-11 | spec convention surfaces candidates nobody accepts | checks-behind-checks | GAP CONFIRMED — the dead end is by explicit design |
| A5-7 | A4-12 | boundary classifier: five verdicts, unmeasurable metric | checks-behind-checks | shape 1 SUPERSEDED (`9f05579` tagging); shapes 2–3 GAP CONFIRMED |
| A5-8 | A4-13 | `vlt-lint-full` asks LLM scanners for exact facts | checks-behind-checks | seam CONFIRMED; both defect instances GAP CONFIRMED |
| A5-9 | A4-14 | `spec_candidate` revision signal is template boilerplate | checks-behind-checks | heading-leg defect GAP CONFIRMED, sharper root than filed |
| A5-10 | A4-15 | activation contract read is the boot whale | boot-cost | CONFIRMED throughout; token band adjusted |
| A5-11 | A4-16 | hand-offs and consults repay the full boot | boot-cost | both claims CONFIRMED (consult half at filed "plausibly") |
| A5-12 | A4-17 | beat 2 orient scales with vault age | boot-cost | CONFIRMED with two sharpenings |
| A5-13 | A4-18 | no instrument measures session token cost | boot-cost | GAP CONFIRMED by exhaustive absence check |
| A5-14 | A4-19 | whale files carry restated weight | boot-cost | CONFIRMED with figure corrections |
| A5-15 | A4-20 | adoption stamp unreachable beyond mint | writes-no-surface | GAP CONFIRMED — reachability gap + internal inconsistency |
| A5-16 | A4-21 | entity-collision coverage pair-incomplete | writes-no-surface | GAP CONFIRMED — greedy cluster consumption; **carries the inherited A4-4 (5) FAILED debt** |
| A5-17 | A4-22 | sayari upgrade omitted convention-adoption line | writes-no-surface | GAP CONFIRMED as reliability + PROVENANCE CORRECTION relocating the fix |
| A5-18 | A4-23 | lint report block is never persisted | writes-no-surface | GAP CONFIRMED + PROVENANCE CORRECTION on which keys are Arc-4-new |

## Capture — this run (grounded against module source 2026-07-29, v0.8.0 @ `9f05579`)

One item: the Arc-3 → Arc-5 transfer, which Arc 4's hand-off explicitly left for this
capture to own ("not captured this run — Arc 5's capture owns them").

### A5-19. The enforcement kit, derive-first edition (build-17 transfer) (2026-07-06) — 2026-07-06-091003-enforcement-kit-derive-first.md

**Provenance.** Filed 2026-07-06 from vlt-core; captured in Arc 3; briefless and unbuilt
through two arcs (evidence-blocked); **owner ruled 2026-07-26: FOLDED INTO ARC 5** — Arc 3
closed with the build unshipped, stated plainly. The binding Arc-3 record (capture, remit
adjustments, parked rulings) lives in `skills/reports/archive/inbox-evolution-arc3-roadmap.md`;
this entry grounds the **delta** — what four releases (0.5.x → 0.8.0) did under the filing —
and does not restate that record.

**Core claim — GAP CONFIRMED, and strengthened at HEAD.** "Vault state lives in prose, so
nothing can count, trigger, or trip" still holds: `module.yaml`'s `vault_structure` map
(`skills/vlt-setup/assets/module.yaml:39`) carries no `tripwires:` entry; no vitals script
exists anywhere in `skills/`; `vlt-setup/SKILL.md` has **zero** hook-install or
`settings.json` capability (the filing's latent bug 3 — "the kit's moment is currently
unprovisionable module-side" — is intact); `vlt-dispatch`'s `ledger` mode
(`skills/vlt-dispatch/SKILL.md:297`) has no Tripped-wires section; nothing counts ingests —
`vlt-lint`'s description still says "proactively after several ingestions"
(`skills/vlt-lint/SKILL.md:3`) with no counter behind it. **Strengthened:** shipped prose
now names the kit as a promised mechanism — `vlt-lint:62` ("escalation of an aging queue is
a tripwire concern, not lint's"), `vlt-lint:104` ("a tripwire concern (the enforcement kit),
not lint's"), `frontmatter.md:231` (`enforcement_counter: <optional until the enforcement
kit lands; then its metric ids are the only legal values>`). The kit is no longer just a
missing feature; it is a **dangling referent in three shipped surfaces**.

**SUPERSEDED in part — build-16 shipped the deferral vocabulary, not the machinery.** The
filing's change 7 ("no deferral without a wire" at mint/council moments) landed in a
different form: the *Enforcement declaration* schema
(`conventions/frontmatter.md:225-240`) requires every convention to carry a stage or a
complete tripwired deferral (`deferral_metric` + `deferral_threshold` + `review_after`),
`vlt-mint:42`/`:152` enforce it at mint moments, and `vlt-lint:80` checks it
(`declared_untripwired`, `deferral_invalid`, `deferral_expired`). What shipped is the
**frontmatter-declared wire**; what did not ship is the **registry**
(`_agent/tripwires.yaml`), the **derivation** (vitals), the **moment** (session strip), and
the **surface** (ledger section) — i.e. everything that would make a declared wire actually
trip. A brief must reconcile the two forms rather than ship a parallel wire vocabulary:
per-convention deferrals live in frontmatter today; the filing's registry serves
vault-level metrics (lint debt, pointer age) that belong to no single convention.

**Dependencies now satisfied (both were open at filing time).**
- `review_after:` shipped (`frontmatter.md:117`, `vlt-lint:62` `review_due`) — the
  `expired_pages` vital is now derivable exactly as the filing anticipated.
- The spec convention shipped (build-15; `conventions/spec.md`) — the sequencing note
  "the tripwires registry should adopt the spec convention's format" is now satisfiable at
  brief time, no longer a fork guard.

**Log grammar (change 4) — largely SUPERSEDED; residual is a tighten, not a section.** The
operating contract's `## The {log}` section (`vault-operating-contract.md:114-137`) already
declares the canonical one-line format matching the filing's strict form, makes the partner
paren omittable only for partner-less generic-agent operations (the filing's forward rule,
by another door), and rules new log types keep the shape (`:124`). Residual: the section
says "Keep it parseable" (`:116`) but does not declare itself a machine-read grammar nor
mandate the case-insensitive, paren-tolerant parser for history (the filing's probe: strict
parsing silently drops ~5% of real headers). Change 4 shrinks accordingly.

**Latent bug 1 — CONFIRMED, still unpinned.** `vlt-lint` Step 0 scopes by filesystem mtime
(`find … -newermt`, `vlt-lint:32`) while the filing's lint-debt metric counts log `ingest`
headers; the two "since last lint" definitions can still disagree and no shipped line pins
one definition per consumer.

**Cross-filing relationships (new since Arc 3 — material for ideation).**
- **The boot-cost thread (A5-10..A5-14) and the kit's moment collide.** The kit's change 3
  adds a SessionStart hook — a per-session cost — exactly while A5-10..A5-14 put the
  existing per-session boot under a microscope. And A5-13's missing instrument is
  vitals-shaped: both want a small derive-only reader over existing records. Ideation
  should rule whether they share one measurement substrate before either is briefed.
- **A5-3 and A5-7 are the kit's own anatomy elsewhere** — a report slot no check fills, a
  classifier bell that has never rung: defined-but-unenforced, the filing's named failure
  shape. Whether their fixes ride the kit or precede it is a grouping ruling.
- **A5-18 (lint report never persisted) touches the vitals substrate** — the derivation
  reads `{log}` lint headers (which do persist), but a persisted report block would widen
  what is derivable without new counters. Non-blocking either way.
- **Parked Arc-3 rulings travel with the build** (binding text in the Arc-3 archive): the
  remit is "unchanged minus the two re-homed pieces"; `days_since_lint` display-only and
  the `blocked_on` shape question were parked on build-17 and re-open at its brief.

**Open design questions — carried, with two grounding notes.** The filing's four questions
carry verbatim (module-owned code home; one metric vocabulary or two; `days_since_lint`
seed wire or display-only; `blocked_on` shape). Note on Q2: shipped source has since
**pre-answered it** in the filing's proposed direction — `frontmatter.md:231` already rules
tripwire metric ids "the only legal values" for `enforcement_counter:` once the kit lands;
a brief inherits that as settled unless ideation overturns it. Note on Q4: `review_after:`
now exists, so the `external-event` companion-date question has its dovetail partner live.

**Evidence debt — 091003 M0 (transfers with the build, per Arc 4's hand-off).** The
counter-accuracy audit + tripwire-hit data remain **unpayable until counters exist**; the
Arc-3 ruling stands: **re-read at build-17 brief time**, not treated as a capture-time
blocker. Residual scope after grounding: the kit's four pieces (vitals reader, registry,
moment, surface) are all still to build, but a brief starts from a smaller, sharper base —
vocabulary shipped, dependencies live, two open questions effectively narrowed.

## Carried forward into Arc 5 (re-listed 2026-07-29, per the archived hand-off contract)

The Arc-4 roadmap's *Carried forward past Arc 4* section says the next `inbox-capture`
re-lists it — "anything left off is silently dropped." Re-listed here in full; the archives
hold the binding text. **None of these is discharged by this capture.**

- **Inherited debt (the build-20 form): A4-4 clause (5), graded FAILED** 2026-07-27,
  owner-confirmed 2026-07-29 — entity-collision coverage is silently pair-incomplete (found
  the Seahawks instance, missed its explicitly-paired Rams twin, `los-angeles-rams.md:77`).
  Filed as `inbox/2026-07-29-120002-…` and captured as **A5-16** (grounded as A4-21 in the
  Arc-4 archive); the fix rides this arc.
  Its filing `inbox/2026-07-25-160949-auto-caption-name-substitution.md` is **held in the
  active inbox as the debt's carrier**. The Arc-4 ledger's `[x]` count may not be read as a
  measure of what that arc proved.
- **Standing watch (released, not verified — none may be read as passed): the sayari
  lint-render halves.** A4-1 (6), A4-3 (7), A4-4 (8, run half) — install halves verified on
  sayari at 0.8.0; **trigger:** the first vlt-sayari full `vlt-lint`. Field-contingent,
  non-gating.
- **Standing watch: the consult channel's refusal path.** A4-5 clause (4) — a non-`answer`
  consult return is still unexercised. When consults only ever return `answer`, that is the
  confabulated-authority signature: inspect, don't assume.
- **Re-listed Arc-3 carry-forwards** (binding text in the Arc-3 archive; digest re-listed in
  the Arc-4 archive's *Carried forward past Arc 3 — re-listed (2026-07-29)*): six standing
  watches (build-19 empty-`{specs}` negative case; build-18 F3; build-21 A3-14; build-22 F1;
  build-22 F2; build-23 F4-in-the-field); the **staged** build-18 F1 `skill_asset_divergence`
  detect (whether the staged edit rode the 0.7.0→0.8.0 upgrade is not recorded — if not, it
  re-arms for the next upgrade); the **dated watch 2026-08-17** (091006 first review-cycle
  evidence, `spec.md:16`, fires without owner action); design debts **Q21 / Q27 / Q24** +
  build-23's vault-side conformance spot-check; the **standing 091002 packaging metric**
  (zero packaging filings for releases ≥0.6.0 — HOLDING through 0.8.0); the **owner action**
  to file the BMB template drift (`after,before`) upstream to BMAD-METHOD.
- **The Arc-3 transfers are now captured** — build-17 + 091003 M0 entered this roadmap as
  **A5-19** above; they no longer sit in the transfer bucket.

## Ideation rulings — A5-1..A5-19 (owner-steered, 2026-07-29)

Rulings below are the owner's; briefs cite this section, never re-litigate. **Session OPEN —
every slot below is unfilled.** The unresolved question pool for this batch is the per-filing
**Open design question (verbatim)** blocks in the Arc-4 archive's capture section
(`skills/reports/archive/inbox-evolution-arc4-roadmap.md`, §*Capture — 18 filings*, A4-6..A4-23
— resolve ids through the mapping table above) plus A5-19's four carried questions in this
roadmap's own capture entry; each question ends this session either ruled here, or explicitly
left to brief time, per build. A slot left empty is honest — do not fill it with a guess, and
do not let a brief infer one. **SESSION CLOSED 2026-07-29 — every slot below is filled:** four
pre-ideation rulings, four cross-filing rulings, the B5-* numbering amendment (recorded at the
re-slug ruling above), grouping & order (nine builds, B5-1..B5-9, ship order set), spikes
(none opened; two pre-brief reads pinned), three evidence debts attached, and every remaining
pool question designated to a brief. Rulings were owner-adopted from clerk recommendations
throughout (delegation precedent: the Arc-3 ship-order slot); the numbering amendment and the
grouping's two judgment calls (A5-7/A5-18 ride B5-9; A5-17 pairs into B5-5) were put to the
owner explicitly.

**Sequencing note (not a ruling):** the four pre-ideation questions sat upstream of grouping
and were ruled first (below); grouping followed in-session.

**Grouping & order** — **RULED 2026-07-29 (owner accepted the clerk's laid-out grouping as-is;
numbering per the same-day B5-* amendment to the re-slug ruling above).** NINE builds over the
sixteen build-bearing filings; A5-1 and A5-5's residues leave the build plan entirely (factory
process records, per the ships-decides ruling below — nothing in this table owns them).
Numbers follow intended ship order at assignment, per the Arc-3 convention (a build that later
slips does not renumber).

| Build | Filings | Subject & notes |
|---|---|---|
| **B5-1** | A5-13 | **The instrument.** Token-cost measurer; **lays the shared substrate** (pre-ideation ruling 1). Ships first — everything behind the measure-first gate waits on it; disposition-(d)-shaped at minimum. |
| **B5-2** | A5-16 | **The collision debt.** Cluster-aware second pass in `vlt-lint-full.js`; carries the inherited A4-4 (5) FAILED debt (A4-1 precedent: inherited debt ships early). |
| **B5-3** | A5-8 + A5-3 | **Exact facts.** Deterministic re-cut of the lint-full LLM-asserted checks + A5-3's slot disposition. As the earliest-shipping proxy-family build, **writes the extended proxy-check rule** the later briefs cite (cross-filing ruling; A4-2 pattern). |
| **B5-4** | A5-6 + A5-9 + A5-15 | **The spec loop.** Acceptance beat / named next owner (module home of the named-slot principle), revision-signal fix, stamp's second writer — one `spec.md` `version:` bump + one four-consumer re-ack covers all three. |
| **B5-5** | A5-2 + A5-17 | **Preserve & report.** `vlt-upgrade` honesty pair: preserve-set widened to provenance + Verify field-completeness. **Ruled with the grouping:** A5-17 pairs here by shared skill, not with A5-15. |
| **B5-6** | A5-4 | **The decision-log convention.** Alone — the promotion ruling ((b), above) sized it into a real build: promote, handshake, lint read-before-flag, upgrade reconcile, Q21 discharge. |
| **B5-7** | A5-10 + A5-11 + A5-12 | **The boot diet.** One cost model, one brief. **Gated:** opens only after B5-1's numbers land AND the pre-hoc/post-hoc ruling is made (its recorded moment). |
| **B5-8** | A5-14 | **The whale re-cut.** Apart from B5-7: disposition (a) is the structurally largest change and carries the `package-lint.py` pre-brief check. Same gate as B5-7. |
| **B5-9** | A5-19 + A5-7 + A5-18 | **The enforcement kit.** Registry, moment, surface — **extends B5-1's substrate, never lays a second one**. **Ruled with the grouping (capture's ride-or-precede question):** A5-7's never-rung streak and A5-18's persistence home RIDE the kit. Last: biggest, most gated; its brief re-reads the 091003 M0 debt + parked Arc-3 rulings. |

**Ship order — SET 2026-07-29: `B5-1 → B5-2 → … → B5-9` as numbered**, with B5-1/B5-2
interchangeable up front, B5-7/B5-8 floating behind the measurement gate, B5-9 last.

Capture's observations that bear on grouping, carried as **capture's material, unaccepted** —
none of these is a proposal, and the capture made no grouping proposal:

- Six cross-filing threads named at capture (archive §*Cross-filing threads*): the
  proxy-check family (A5-3, A5-6, A5-7, A5-8, A5-9), the write-surface pair (A5-15, A5-18),
  the reporter dependency (A5-17 → A5-15), the boot-cost anatomy (A5-10..A5-14, deliberately
  factored — A5-13 the measurement keystone), the factory thread (A5-1, A5-5, and A5-3's tail
  question), the upgrade-machinery pair (A5-2, A5-4).
- A5-19's capture entry: whether A5-3/A5-7's fixes ride the enforcement kit or precede it is
  a grouping ruling; A5-18 touches the vitals substrate but is non-blocking either way.
- A5-5's "named slot" idea and A5-6's "named next owner" idea are the same design at two
  altitudes — plausibly one ideation ruling.

**Pre-ideation rulings the capture demanded** — all four **RULED 2026-07-29** (owner adopted
the clerk's recommendations, per the delegation precedent of the Arc-3 ship-order slot):

1. **The measurement-substrate collision (A5-13 / A5-19) — RULED: ONE shared substrate.**
   One derive-only reader over existing records serves both the token-cost instrument and
   the enforcement kit's vitals; whichever build lands first lays it, the other extends it.
   Rationale on record: A5-13's missing instrument "is vitals-shaped" (capture), and a
   second parser over the same `{log}` grammar re-creates the two-definitions-of-one-metric
   class A5-19's latent bug 1 already documents.
2. **Instrument-before-briefs sequencing (A5-13) — RULED: MEASURE FIRST.** The instrument
   runs against the work vault (a one-shot measurement in disposition (d)'s form suffices;
   the full build need not ship first) **before** the boot-cost filings (A5-10..A5-14) are
   briefed, so their dispositions are chosen against numbers. Three signals concurred
   (Arc-4 hand-off, A5-13's own preference, A5-12's measure-first deferral); the
   counter-lesson is build-20's sized-by-assumption failed clause.
3. **The proxy-check framing (A5-3, A5-6, A5-7, A5-8, A5-9) — RULED: ONE cross-cutting
   finding, per-filing fixes.** One framing ruling — the honest-reporting rule extended
   from report *slots* to *checks*: a check must be able to state what it actually
   measures — written once and cited by the briefs, per the A4-2 pattern (one build writes
   the general rule; the others cite it). The five fixes still land wherever grouping
   assigns them; which build writes the extended rule is a grouping call.
4. **The factory-vs-module fork (A5-1, A5-5 item 3, A5-3's tail) — RULED: the
   SHIPS-DECIDES principle.** The discriminator is ruled, not the cases: work whose
   artifact is **tracked and public** (`tools/` — part of the release contract) gets an
   arc build, per the 091002 precedent; work whose artifact is **gitignored factory
   tooling** (`.claude/skills/`, CLAUDE.md, brief templates) gets a factory process
   record and **no module build**, per the A3-18 precedent. Applied: A5-1 → factory
   record, no build; A5-5 item 3 → factory record, no build; A5-3's tail
   (report-contract lint, `package-lint.py` territory) → ordinary arc work if taken up.

**Cross-filing decide-once rulings** — all four **RULED 2026-07-29** (owner adopted the
clerk's recommendations, same delegation basis as the pre-ideation slot):

- **Pre-hoc vs post-hoc governance (A5-10's verbatim question, arc-level) — RULED:
  DEFERRED UNTIL THE NUMBERS LAND, as a dated deferral with a named moment, not an
  omission.** The trade is ruled **as the first act of boot-cost briefing**, against
  A5-13's measured numbers — sequencing already holds those briefs behind the instrument
  (pre-ideation ruling 2), so the deferral costs nothing, and the trade is exactly what
  the instrument exists to price. No boot-cost brief may open without this ruling on
  record.
  **→ RULED 2026-07-30 (owner, at the named moment, against the landed numbers —
  vlt-core baseline `cost-baseline-2026-07-29.md` + the work-vault run
  `inbox/2026-07-30-111133-b5-1-work-vault-cost-manifest-run.md`): POST-HOC DEFAULT.**
  Pre-hoc carries only what is **identity-bearing** (the ceremony beat — the identity
  ruling holds; the question is what the beat *reads*, not whether it exists) or
  **act-blocking** (rules whose violation post-hoc enforcement cannot cheaply undo —
  destructive/durability-class writes); **everything else is enforced post-hoc by
  `vlt-lint`/review-council** (optimistic execution, cheap correction). Priced on: the
  contract read is ~77% of every partner's fixed boot (38,271 chars ≈ 7.3–9.6k est.
  tokens, repaid on hand-offs/consults per A5-11); conventions already run point-of-use
  with no observed correctness cost; the post-hoc side strengthened this arc (B5-3
  exact facts, B5-6 lint memory, write-verification@2, B5-9 kit incoming). This is the
  principle only — **B5-7/B5-8's briefs choose mechanisms under it** (A5-10's (a)/(b)
  digest-vs-TOC choice stays a B5-7 brief-time question; (c) is foreclosed). The
  measure-first gate is now OPEN.
- **The named-slot design (A5-5 item 3 + A5-6 direction 1) — RULED: ONE principle, TWO
  homes.** The shared principle is ruled once, here: **a standing obligation gets a named
  slot and a named owner, never an unnamed one.** The factory instance (verification
  template's handshake slot; capture's narrowing applies — of §7's "other standing
  rituals," only Scrub is a §7 ritual today) lands in the factory process record, no
  build, per ships-decides; the module instance (`spec_candidate` named next owner +
  promotion step) lands in whatever build takes A5-6. Both cite this principle.
- **Amended filings procedurally (A5-9's verbatim tail) — RULED: amendments RIDE IN
  PLACE.** An amendment rides its filing; the original timestamp stays the anchor and
  capture grounds the amended whole — as the A5-9 addendum was in fact handled. Standing
  rule for future capture runs.
- **A5-4's homing choice (id corrected this session — the skeleton mislabeled it A5-9;
  the decision-log homing question is A5-4's, archive id A4-9) — RULED: (b), PROMOTE to
  `{conventions}/decision-log.md`** with `version:`/`consumers:` per build-4, re-acking
  consumers. Confirms the owner's 2026-07-26 instinct as a ruling: correct by handshake
  doctrine, and it homes the `vlt-mint:84` three-homes supersession debt (Q21), which
  makes it cheaper than it first appears. Sizes A5-4's build accordingly.

**Spike obligations** — **RULED 2026-07-29: NO spikes opened.** No external-source unknowns
were flagged in this batch. The two read-before-brief obligations the captures name are
pinned to their builds so they cannot be lost: the **`package-lint.py`
packaging-expectations check** pins to **B5-8's brief** (before any progressive-disclosure
re-cut is scoped); the **091003 M0 + parked Arc-3 rulings re-read** pins to **B5-9's brief**.

**Evidence-debt dispositions** — **RULED 2026-07-29, all three attached:**

- **091003 M0 (counter-accuracy audit + tripwire-hit data)** → attaches to **B5-9**;
  re-read at brief time per the standing Arc-3 ruling; **not a blocker** (unpayable until
  counters exist).
- **The inherited A4-4 clause (5) FAILED debt** → attaches to **B5-2**; its carrier filing
  (`inbox/2026-07-25-160949-auto-caption-name-substitution.md`) stays in the active inbox
  until the fix ships and passes acceptance.
- **A5-7 shape 2 (discriminating probe)** → **NOT-BLOCKING for B5-9**; remains a
  field/owner action — the streak reporting B5-9 ships does not need the probe to exist.

**Questions deliberately left to brief time** — **RULED 2026-07-29, designated per build;
each brief owns its questions, none stays cross-cutting:**

- **B5-2:** A5-16's cluster-aware second-pass shape (carried as a proposal, not a ruling).
- **B5-3:** A5-3's (a)/(b)/(c) slot disposition.
- **B5-4:** A5-6's three candidate directions; A5-15's second-writer vs mint-borne-only
  scope (including where a second stamp write is allowed to land).
- **B5-5:** A5-2's broaden-vs-add on `mints_preserved:`; A5-17's Verify-side check vs
  template-side required markers.
- **B5-7:** A5-11's consult-boot bump-or-no-bump call (capture's read: likely no bump).
- **B5-9:** A5-18's persistence home under the derive-first rule; A5-19's parked
  `days_since_lint` and `blocked_on` questions (re-open at its brief per the Arc-3 ruling).

## Deferred acceptance ledger (Arc 5)

Per convention, acceptance rides the next ordinary vlt-core upgrade (or, for factory-side
builds, the named factory/owner event); per-build checks are appended at brief time. Every
check carries its brief-time tag — **only ship-verifiable checks gate `arc-closeout`**.

- [x] **B5-1 (cost-instrument, briefed 2026-07-29):** (1) **[ship-verifiable]** the
  baseline lands and is trustworthy — `tools/cost-manifest.py` has run in both modes
  (module mode at the build's HEAD, vault mode against vlt-core) and the dated baseline
  report is on disk in `skills/reports/` with the gate-artifact header and the honest
  not-the-work-vault statement; ≥3 vault-mode figures hand-verified against independent
  `wc -c`/`grep -c` on vlt-core. Bounded — the factory runs it the day the build lands;
  B5-7/B5-8's briefs cite the artifact as their measure-first gate.
  (2) **[field-contingent]** the work vault's numbers — the owner runs vault mode on the
  **work-machine consumer vault** (the originating signal's vault; the factory machine
  cannot read it) and files the output back. Non-gating at closeout; per pre-ideation
  ruling 2, B5-7's brief should not choose dispositions without these numbers if they
  exist by then.
  → **DISCHARGED 2026-07-30** (acceptance-discharge run): (1) the baseline artifact is on
  disk — `skills/reports/cost-baseline-2026-07-29.md` with the gate-artifact header and the
  honest not-the-work-vault statement (:1–16), both modes run, 4 figures hand-verified at
  build. (2) the work-vault run is filed —
  `inbox/2026-07-30-111133-b5-1-work-vault-cost-manifest-run.md`: vlt-sayari measured
  directly (the filing itself records that the factory-cannot-read premise no longer held,
  so the *named* vault was measured, not a proxy), read-only-proven (1,317-file mtime
  snapshot), figures spot-verified. Both clauses evidenced; carrier filing archived.
- [ ] **B5-2 (collision-debt, briefed 2026-07-29):** (1) **[ship-verifiable]** both halves
  of the fix reach the field — on the next ordinary vlt-core upgrade, the installed
  `.claude/workflows/vlt-lint-full.js` carries the callout-seeded pass
  (`name_callout_targets` in PAGE_SCAN + the pair pass + `entity_scan_facts` in the
  return) and the installed `vlt-lint/SKILL.md` carries the Step 2 second-leg paragraph,
  the widened Step 5 `entity_scan:` denominator, and the Step 4 duplicate-filing guard —
  checkable by grep on the installed vault. Bounded — the upgrade happens anyway.
  (2) **[field-contingent]** the named lost pair surfaces — **the inherited A4-4
  clause (5) debt's discharge**. Producing vault: vlt-core (factory-readable; the owner
  runs the lint). On the first full `vlt-lint` after the upgrade, with the "Which
  Jackson?" callout (`los-angeles-rams.md:76`) still standing: the los-angeles-rams ↔
  nfl-2026-offense-rankings pair surfaces in `entity_collisions` marked
  `(callout-seeded)`; `entity_scan:` carries the seeded-pair count (R ≥ 1); and no
  duplicate backlog item is filed (the pair is already tracked — the Step 4 guard's first
  live exercise). If the callout resolves before any full lint runs, the check re-targets
  the next standing name callout (never vacuously passed — the full rule is in the
  brief's Acceptance). On pass, the debt discharges and the carrier filing
  (`inbox/2026-07-25-160949-auto-caption-name-substitution.md`) plus this build's filing
  become archivable per `arc-closeout` Stage 5.
  → Upgrade-side DISCHARGED 2026-07-30 (vlt-core 0.8.0→0.9.0, ledger [2026-07-30 13:20]):
  installed `vlt-lint-full.js` carries `name_callout_targets` (×4) + the pair pass +
  `entity_scan_facts`; the SKILL surfaces are present in their post-B5-8 homes (second-leg +
  widened `entity_scan:` denominator in `references/checks.md`/`report.md`, duplicate-filing
  guard in `references/fix-and-file.md` — the re-cut moved homes, substance intact).
  STILL-OPEN: (2) the named lost pair surfaces (callout-seeded, R ≥ 1, no duplicate filing)
  — the inherited A4-4 clause (5) debt; trigger: the owner's first **full** `vlt-lint` on
  vlt-core with the "Which Jackson?" callout still standing.
- [ ] **B5-3 (exact-facts, briefed 2026-07-29):** (1) **[ship-verifiable]** the exact-facts
  re-cut reaches the field — on the next ordinary vlt-core upgrade, the installed
  `.claude/workflows/vlt-lint-full.js` carries `normalizeTarget`, the `stubSlugs` channel,
  and computed summary/category/attestation verdicts (no
  `summary_issue`/`category_violations`/`sources_count` in its schemas); the installed
  `vlt-lint/SKILL.md` carries the tier-1 sources-vs-prose check, `stubSlugs` at Step 0, and
  a Step-5 fence parsing as strict YAML whole; the installed `write-verification.md` is
  `version: 2` with all four installed consumers acking `@2`; the installed contract carries
  the extended proxy-check rule. Grep/parse-checkable on the installed vault; bounded — the
  upgrade happens anyway.
  (2) **[field-contingent]** the deterministic facts behave on real data — on the first full
  (>~30-page) `vlt-lint` after the upgrade: (a) `missing_targets` contains no entry whose
  target actually resolves after normalization (the 82% alias/anchor/prefix class —
  spot-check every reported entry) and no index-registered stub; (b) `near_duplicates`
  contains no pair whose only secondary signal is a stem shared by ≥4 pages; (c)
  `sources_vs_prose_mismatches` has a producer in the run's mode — and on the first *scoped*
  lint after the upgrade, the tier-1 pass fills or honestly empties it. Producing vault:
  **vlt-core** (factory-readable; the owner runs the lint — both scoped and full runs occur
  in ordinary use, so the event is expected but not caused by the upgrade).
  → Upgrade-side DISCHARGED 2026-07-30: installed workflow carries `normalizeTarget` (×6) +
  `stubSlugs` (×3) and zero `summary_issue`/`category_violations`/`sources_count`; the
  tier-1 sources-vs-prose check and `stubSlugs` are in the installed lint dir (post-re-cut
  homes: `references/checks.md`/`report.md`/`full-scale.md`); installed
  `write-verification.md` is `version: 2` with all four consumers acking `@2`
  (extract/ingest/lint/research); the installed contract carries the proxy-check rule; the
  Step-5 fence (now `references/report.md`) parses whole as strict YAML (11 top-level
  keys). STILL-OPEN: (2)(a)/(b) grade on the first full lint, (c) on the first scoped lint
  — trigger: the owner's ordinary lint cadence on vlt-core.

- [ ] **B5-4 (spec-loop, briefed 2026-07-29):** (1) **[ship-verifiable]** the spec loop
  reaches the field — on the next ordinary vlt-core upgrade, the installed `spec.md` is
  `version: 2` carrying the *Promotion from candidate* section and the deferral blind-spot
  statement, with all four installed consumers acking `spec@2`; the installed
  `vlt-lint/SKILL.md` carries the dated-revision-record signal (no "What changed" heading
  match), the named-owner backlog shape, and the `spec_candidate:` + `consult_retroactive:`
  report keys; the installed `vlt-upgrade/SKILL.md` carries the re-cut retrofit signal, the
  retrofit stamp beat, the adoption-aware pre-flight compare + apply carry-forward, and the
  aged adoption line; the installed `vlt-mint` carries the authorized-writers rule and the
  installed `vlt-dispatch` the consult stamp beat. Grep-checkable on the installed vault;
  bounded — the upgrade happens anyway (and its own post-flight adoption line exercises the
  declared-since age live).
  (2) **[field-contingent]** the loop closes on real candidates — producing vault:
  **vlt-core** (factory-readable; the owner runs lint; the filing's four relay-leg candidates
  and two retroactive consults are already standing, so the events are expected in ordinary
  use). On the first full `vlt-lint` after the upgrade: (a) no candidate surfaces on a bare
  "What changed" heading (the seven template docs stay unflagged; relay-leg candidates still
  surface, each with a named owner in its backlog item); (b) the two known retroactive
  consults report `consult_retroactive`, not silence; (c) when the owner promotes or declines
  any one candidate, the loop terminates — promotion lands the doc in `{specs}` and stamps
  `spec.md`'s `adoption_first_instance:` (surviving the next upgrade un-clobbered), or a
  decline is recorded and the next lint honors it (no re-file). Clause (c) is the owner's
  decision, not caused by the upgrade — if unruled by closeout it stays open as this check's
  unbounded tail while (a)/(b) grade on the first lint.
  → Upgrade-side DISCHARGED 2026-07-30: installed `spec.md` is `version: 2` carrying
  "Promotion from candidate" (:79), all four consumers ack `spec@2`
  (dispatch/lint/mint/upgrade); the installed lint carries the `spec_candidate:` +
  `consult_retroactive:` keys (`references/report.md`, `fix-and-file.md`); the installed
  upgrade carries the adoption-aware compare + carry-forward, and the **aged adoption line
  fired live** — the upgrade ledger's Convention adoption line reports declared-since ages
  (spec: 24 days). STILL-OPEN: (2)(a)/(b) on the first full lint (owner cadence); (c) the
  owner's promote/decline ruling — the unbounded owner tail. NOTE the upgrade's own
  Attention line: `spec.md` reads `adoption_first_instance: null` while two live specs sit
  in `_agent/specs/` (retrofitted pre-rule) — the backfill stamp is an owner ceremony
  ruling adjacent to (c).

- [ ] **B5-5 (preserve-and-report, briefed 2026-07-29):** (1) **[ship-verifiable]** the
  widened preserve set and the completeness net reach the field and self-exercise — on the
  next ordinary vlt-core upgrade: (a) the installed `vlt-upgrade/SKILL.md` carries the
  *Local mints* provenance bullet, the widened Step-2 prohibition + B2/confirm wording, the
  broadened `mints_preserved:` report/ledger descriptions, the template's required-lines
  annotation, and the Verify field-completeness check — grep-checkable on the installed
  vault; (b) that upgrade's Step-1 snapshot and `mints_preserved:`/ledger line name
  vlt-core's standing vault-grown op skill (`vlt-file-feedback`) as a local mint — the
  widened path is forced by the standing dir, and under 0.8.0 wording it would be omitted,
  so the clause can fail; (c) the upgrade's own ledger entry carries every required template
  line, Convention adoption included — the completeness check's first live exercise. Bounded
  — the upgrade happens anyway.
  (2) **[field-contingent]** the reliability fix holds where it failed — the next
  **vlt-sayari** upgrade's ledger entry carries the Convention adoption line (and every
  required template line): the execution shape that dropped the line at 0.8.0 (A4-2
  clause-7's adoption-half FAIL), re-run under the completeness check. Producing vault:
  vlt-sayari on the **work machine — the factory cannot read it**; the owner runs that
  upgrade on their own cadence and files the ledger evidence back (the B5-1 check-2
  pattern). Non-gating at closeout.
  → Upgrade-side DISCHARGED 2026-07-30: (a) all named surfaces grep-present in the
  installed `vlt-upgrade/SKILL.md`; (b) the widened path **fired** — the upgrade's
  pre-flight snapshot and `Mints preserved:` line both name `vlt-file-feedback` alongside
  the four partners (B1 JSON `local_mints_preserved` confirms all 5); (c) the ledger entry
  carries every required template line, Convention adoption included — the completeness
  check's first live exercise, passed. STILL-OPEN: (2) the next vlt-sayari upgrade's ledger
  carries the adoption line — trigger: the owner runs that upgrade (note: vlt-sayari is now
  factory-readable per the B5-1 check-2 filing, so its ledger can be read directly).

- [ ] **B5-6 (decision-log-convention, briefed 2026-07-29):** (1) **[ship-verifiable]** the
  promoted convention reaches the field and its migration self-exercises — on the next
  ordinary vlt-core upgrade: (a) the installed `_meta/conventions/decision-log.md` exists
  at `version: 1` carrying the `ref:`-bearing entry schema, the supersession idiom with the
  `wiki-supersession.md` principle citation, and `checked`/`vlt-lint`/lint-run enforcement
  frontmatter, with its baseline copy in `{overlays}/.baseline/`; (b) the installed
  `vlt-mint`, `vlt-upgrade`, and `vlt-lint` all ack `decision-log@1`, `vlt-mint`'s section
  and the seed template are the pointer re-cuts (the schema fence greps to exactly one
  installed home), and `vlt-lint` carries the read-before-flag preface, the write-through
  beat, and the `governance_memory:`/`rulings_recorded:` keys; (c) the vault's existing
  `_agent/mint/decision-log.md` is untouched, and that upgrade's own reconcile pass runs
  the extended leg against the vault's real log — pre-schema and pre-`ref:` entries surface
  as cannot-classify rather than silently swept, and any standing overlay/base divergence
  with no `ref:`-keyed entry surfaces exactly once for ruling (the migration's first live
  exercise — it can fail). Grep-checkable on the installed vault; bounded — the upgrade
  happens anyway.
  (2) **[field-contingent]** the memory closes a real loop — producing vault: **vlt-core**
  (factory-readable; the owner runs lint on its ordinary cadence, but the loop needs two
  sweeps AND an owner ruling in between, which nothing in the build or upgrade causes). On
  the first lint after the upgrade that surfaces a long-lived governance finding: it
  reports `undisposed` with the `governance_memory:` denominator present; the owner rules;
  the write-through appends a live `ref:`-keyed entry (`rulings_recorded:` names it) — and
  the **next** lint reports the same finding `adjudicated` with its citation instead of
  re-litigating it, the first sweep-to-sweep recall in the check's history. If no
  governance finding is standing at the first lint, the check re-targets the first one
  that arises (never vacuously passed). Non-gating at closeout.
  → Upgrade-side DISCHARGED 2026-07-30: (a) installed `_meta/conventions/decision-log.md`
  is `version: 1` (`ref:` schema ×5, `wiki-supersession` citation ×2), baseline seeded at
  `_agent/conventions/.baseline/decision-log.md`; (b) mint/upgrade/lint all ack
  `decision-log@1`, lint carries read-before-flag + `governance_memory:`/`rulings_recorded:`
  (`references/checks.md`/`report.md`/`fix-and-file.md`); (c) `_agent/mint/decision-log.md`
  untouched (89.5K), and the reconcile's extended leg **ran live** — 18 pre-schema + 5
  pre-`ref:` entries surfaced cannot-classify rather than swept, no undisposed subjects
  found; the write beat also fired once (the enforcement-kit convergence ruling recorded
  via decision-log-write). STILL-OPEN: (2) sweep-to-sweep recall — first lint surfacing a
  governance finding → owner ruling → next lint reports it `adjudicated`; trigger: the
  owner's lint cadence with an owner ruling in between.

- [ ] **B5-7 (boot-diet, briefed 2026-07-30):** (1) **[ship-verifiable]** the boot diet
  reaches the field, both homes in step — on the next ordinary vlt-core upgrade: (a) the
  installed `_meta/vault-rule-card.md` exists, ≤ 8,000 bytes, its `derived_from:` SHA
  matching the installed shipped contract; (b) the three installed partner SKILLs open the
  ceremony on the rule-card (no eager full-contract read survives in any opener), carry the
  bounded Beat-2 list (`{log}` last-5, `{index}` headings-tier, `{backlog}` open items,
  `## Thread` only) and the widened already-fresh skip, with the installed contract's
  ritual section in step on all three counts; (c) the installed `vlt-consult.js` carries
  the lite-boot exemption and the installed `consult.md` is still `version: 1` (no bump —
  Group E clean on the installed handshake surface); (d) at rest at the release,
  `cost-manifest` module mode reports the per-partner fixed boot as SKILL + rule-card,
  ≥60% below the 2026-07-29 baseline's 48,340–49,768-byte rows. Grep/run-checkable;
  bounded — the upgrade happens anyway.
  (2) **[field-contingent]** the diet holds in a live mature-vault session — producing
  vault: **vlt-core** (owner-run, factory-readable; vlt-sayari, now also factory-readable,
  corroborates if its upgrade lands first). On the first interactive partner activation
  after the upgrade: the ceremony read is the rule-card (not the 38K contract), the `{log}`
  orient read is the last-5 slice (not the full file), and no full-file
  `{backlog}`/`{index}` read occurs — owner spot-check of one session's reads. And on the
  first consult after the upgrade: the summoned partner boots lite (no
  contract/rule-card + Beat-2 read by the summoned agent) — the first exercise of the
  exemption. Outcome measure, non-gating: the originating field signal (mature-vault
  sessions feel token-expensive) should stop reproducing; the owner's say-so note closes
  it. Non-gating at closeout.
  → Upgrade-side DISCHARGED 2026-07-30: (a) installed rule-card 5,511 B ≤ 8,000, its
  `derived_from:` SHA `f066909d…` equal to the installed contract's actual sha256 (verified
  by hash run); (b) all three partner openers reference the card (librarian/researcher/
  creative ×1 each), Beat-2 last-5 bound present, governance == shipped per the ledger so
  the contract's ritual section is in step; (c) installed `vlt-consult.js` byte-identical
  to shipped and carries the LITE BOOT exemption (:129), installed `consult.md` still
  `version: 1`; (d) at-rest fixed boot 15,896–17,324 B vs the baseline's 48,340–49,768 B
  rows (~64–67% cut ≥ 60%, recorded at build). STILL-OPEN: (2) first interactive partner
  activation (card-not-contract read, last-5 orient) and first consult (summoned partner
  boots lite) — owner spot-check of one session's reads; plus the owner's say-so note on
  the originating token-expense signal.

- [ ] **B5-8 (whale-recut, briefed 2026-07-30):** (1) **[ship-verifiable]** the re-cut
  reaches the field intact — on the next ordinary vlt-core upgrade: (a) the installed
  `vlt-dispatch` and `vlt-lint` dirs each carry the router `SKILL.md` + `references/*.md`,
  routers within budget (dispatch ≤ 14,000 bytes, lint ≤ 12,000), every reference named by
  its router present on disk and no orphan reference (the router-integrity agreement,
  grep/ls-checkable on the installed vault); (b) the installed routers' `depends_on:` acks
  are byte-identical to 0.8.0's plus B5-3..B5-6's shipped bumps — no convention moved in
  this build — and the installed vault's convention-coherence surface stays clean; (c) the
  installed `references/report.md` Step-5 fence parses whole as strict YAML; (d) at rest
  at the release, `cost-manifest` module mode reports the dir-aware whale aggregates with
  router rows within the budgets. Bounded — the upgrade happens anyway.
  (2) **[field-contingent]** the re-cut behaves and pays in live use — producing vault:
  **vlt-core** (owner-run, factory-readable). On the first post-upgrade single-mode
  dispatch invocation (a `consult` or a `ledger` glance): the session reads the router
  plus that mode's reference only — no other mode's reference is opened (owner spot-check
  of the session's reads). On the first post-upgrade scoped lint: the run never opens
  `references/full-scale.md`, and its report is shape-identical to the pre-cut contract
  (same keys, same fence, the composed denominator lines as before) — the re-cut moved
  text without moving behavior. Outcome measure, non-gating: the dispatch/lint share of
  the originating token-expense signal shrinks on the measured invocation paths; the
  owner's say-so note closes it. Non-gating at closeout.
  → Upgrade-side DISCHARGED 2026-07-30: (a) installed routers 11,318 B (dispatch, ≤ 14,000)
  and 6,946 B (lint, ≤ 12,000 — both a few bytes over their build-time sizes from B5-9's
  in-step additions, still well within budget); router↔references bipartite exact — each
  router names exactly its four references, all eight present on disk, no orphans; (b) no
  convention moved in the build and the installed handshake surface is consistent (Group E
  PASS at release rode the same bytes); (c) installed `references/report.md` Step-5 fence
  parses whole as strict YAML (11 top-level keys); (d) the dir-aware whale aggregates
  recorded at build. STILL-OPEN: (2) first single-mode dispatch reads router + that mode's
  reference only, and first scoped lint never opens `full-scale.md` with a shape-identical
  report — owner spot-checks; plus the owner's say-so on the token share.

- [ ] **B5-9 (enforcement-kit, briefed 2026-07-30):** (1) **[ship-verifiable]** the kit
  reaches the field and self-exercises — on the next ordinary vlt-core upgrade (its
  reconfigure hand-off runs the new vlt-setup §2b): (a) the installed vault carries
  `.claude/hooks/vlt-vitals.py`, a seeded `_agent/tripwires.yaml` with the two stock wires
  (vlt-core has no registry today, so the seed path is forced and can fail), and an
  `_agent/lint-reports/` dir; (b) `{root}/.claude/settings.json` carries the strip hook
  exactly once **and the pre-existing vault-local dirty-tree SessionStart hook
  byte-identical** — the never-clobber merge's first live exercise; (c) the installed
  `frontmatter.md` is `version: 5` with all six installed consumers acking `@5`
  (`vlt-dispatch` included, its first frontmatter ack), the installed lint router carries
  the persist beat + the Step-0 two-definitions pin, `checks.md`'s two tripwire pointers
  resolve to real surfaces, and `ledger.md` carries the Tripped-wires section; (d) run on
  vlt-core, `vlt-vitals.py` derives its figures with ≥3 hand-verified against independent
  `grep -c`/`wc -c`, and `--strip` output matches the registry state. Grep/run-checkable;
  bounded — the upgrade happens anyway.
  (2) **[field-contingent]** the wires live and the M0 debt discharges — producing vault:
  **vlt-core** (factory-readable; the owner runs sessions and lints on ordinary cadence).
  (a) The first interactive session after the upgrade renders the strip correctly for the
  vault's true state (a tripped line matching a hand-run of the reader, or silence when
  green — owner spot-check); (b) the first lint after the upgrade **persists its report
  block** to `_agent/lint-reports/` (the first persisted lint report in the vault's
  history — A5-18's payoff) and its Step-6 log entry resets `ingests_since_lint` by
  derivation on the next vitals run; (c) **the 091003 M0 counter-accuracy audit** — the
  owner hand-counts ingests-since-last-lint and open dispatch pointers once and compares
  against the vitals output; agreement discharges the debt, a discrepancy files back as a
  defect (either outcome closes the audit — it could fail); (d) the first `ledger` glance
  shows the denominated classifier-streak line and the blocked-facet grouping renders
  (untagged bucket at minimum). Non-gating at closeout; tripwire-*hit* data (a wire
  tripping on real debt) is noted when it occurs but is not required — a healthy vault
  never tripping is a pass, not a vacuous one, because clause (d)'s denominated zero and
  clause (a)'s hand-check prove the machinery can see.
  → Upgrade-side DISCHARGED 2026-07-30: (a) `.claude/hooks/vlt-vitals.py` installed,
  `_agent/tripwires.yaml` carries exactly the two stock wires (`lint-debt`,
  `relay-overdue`) — the forced seed path fired as an **owner-ruled** prototype→shipped-
  schema convergence with thresholds kept (ledger, enforcement-kit-convergence migration);
  `_agent/lint-reports/` exists; (b) settings.json carries the vitals strip exactly once
  and the vault-local dirty-tree SessionStart hook entry is preserved (the one replaced
  entry was the prototype `session-health-strip.sh`, superseded by explicit owner ruling —
  the named dirty-tree hook untouched, its script on disk); (c) installed `frontmatter.md`
  is `version: 5` with all six consumers acking `@5` including vlt-dispatch's first ack;
  lint router carries the persist beat; `references/ledger.md` carries Tripped-wires; (d)
  vitals **run live on vlt-core**: 10 metrics derived, 3 hand-verified exact (`log_bytes`
  151,536 = `wc -c`; `backlog_bytes` 103,086 = `wc -c`; `open_pointers` 5 = `grep -c
  '^- \[ \]'`), both wires evaluated denominated (2/10, 12/21), `--strip` silent-when-green
  matching the 0-of-2-tripped registry state, exit 0 both modes. STILL-OPEN: (2)(a) first
  session strip render spot-check; (b) first lint persists its report to
  `_agent/lint-reports/` + Step-6 reset-by-derivation; (c) the 091003 M0 counter-accuracy
  audit (owner hand-count vs vitals — either outcome closes it); (d) first ledger glance
  shows the streak line + blocked-facet grouping. Trigger: owner sessions and lints on
  ordinary cadence.

## Closeout record (2026-07-30, arc-closeout)

**This arc is archived — do not append.**

**Gate:** ledger discharged per the ship-verifiable rule — B5-1 ticked with dated evidence;
B5-2..B5-9's clause (1) all discharged 2026-07-30; every open clause (2) is tagged
`[field-contingent]` and unfired, so it releases to the standing watch register below and
does not block. `v0.9.0` tagged at `f3b343d`, pushed. **The `[x]` count is not a measure of
what the arc proved:** eight of nine items close released-not-ticked over field-contingent
tails that were never exercised.

**Standing watch register (carried forward past Arc 5 — the next arc's `inbox-capture`
re-lists these):**

1. **B5-2 (2) — the A4-4 clause (5) Jackson-pair debt, inherited from Arc 4** — STILL OPEN
   at arc close, carries forward past Arc 5 as inherited debt (its second arc). Trigger:
   the owner's first full `vlt-lint` on vlt-core with the "Which Jackson?" callout
   standing. Carrier filing `inbox/2026-07-25-160949-auto-caption-name-substitution.md`
   plus `inbox/2026-07-29-120002-…` held active.
2. **B5-3 (2)** — the exact facts behave on the first real full + scoped lints (vlt-core).
   Filing `2026-07-26-184704-…` held active.
3. **B5-4 (2)** — (a)/(b) on the first full lint; (c) the owner's promote/decline ruling
   (unbounded owner tail), **plus the flagged owner action:** two live specs sit in
   `_agent/specs/` with `adoption_first_instance: null` — the backfill stamp is a ceremony
   ruling (upgrade Attention line). Filings `142000`/`184705`/`120001` held active.
4. **B5-5 (2)** — the next vlt-sayari upgrade's ledger carries the Convention adoption
   line (sayari now factory-readable, evidence can be read directly). Filing `120003` held
   active; `183003` archived this close (its own clauses fully discharged, the sayari tail
   belongs to `120003` — the per-filing criterion's condition 2).
5. **B5-6 (2)** — first sweep-to-sweep governance recall (lint surfaces a finding → owner
   rules → next lint reports it `adjudicated`). Filing `124223` held active.
6. **B5-7 (2)** — first live partner activation (card-not-contract, last-5 orient) + first
   consult lite-boot, owner spot-check; owner say-so note on the mature-vault
   token-expense signal. Filings `082930`/`082931`/`082932` held active.
7. **B5-8 (2)** — first single-mode dispatch + first scoped lint spot-checks; owner say-so
   on the dispatch/lint token share. Filing `082934` held active.
8. **B5-9 (2)** — first session strip render spot-check; first lint persists its report +
   Step-6 reset-by-derivation; **the 091003 M0 counter-accuracy audit** (owner hand-count
   vs vitals — either outcome closes it); first ledger glance (streak line +
   blocked-facet grouping). Filings `091003`/`120004`/`142500` held active.
9. **Pre-Arc-5 standing items, re-carried:** the vlt-track loop-profile watch (Arc 1); the
   BMB drift upstream filing (owner, Arc 2); lifecycle-audit Item 4 (owner/vault-side
   rail) + Item 7 (watch); the sayari lint-render and non-answer-consult standing watches
   (Arc 4).

**Filing dispositions this close (per-filing criterion, Arc-3 precedent):** archived —
`2026-07-29-082933` + `2026-07-30-111133` (B5-1, moved at discharge), `2026-07-25-183003`
(clauses fully discharged, residual tail attributable to `120003`), and — **owner-ruled at
this close** — `2026-07-25-171500` (A5-1) + `2026-07-26-141500` (A5-5): both ruled no-build
at ideation under ships-decides, their residue living as factory process records; the
build-shipped-and-passed criterion can never fire for them, and holding them would make the
active inbox overstate what is outstanding. Recorded as a ruled extension of the per-filing
criterion: **a filing whose ideation ruling terminates its lifecycle with no build archives
at the close of the arc that ruled it.** All fourteen other Arc-5 filings held active
against the watches above.

## Status & next step

**Position (updated 2026-07-29, ideation CLOSED):** arc stood up; nineteen captures
A5-1..A5-19 grounded @ `9f05579`; carry-forwards re-listed; **ideation complete** — the
rulings section above holds the full record: nine builds **B5-1..B5-9**, ship order set,
all pre-ideation and cross-filing questions ruled, debts attached, remaining questions
designated to briefs. A5-1 and A5-5's residues produce **no module build** (factory
process records, per ships-decides). All nineteen filings remain
in the active inbox (captured ≠ built ≠ accepted).

**B5-1 BRIEFED 2026-07-29** (`skills/reports/build-B5-1-cost-instrument.md`; re-ground
clean — HEAD still `9f05579`, every capture site and figure HOLDS, zero grounding
corrections; its two acceptance checks are in the ledger above).

**B5-1 BUILT 2026-07-29** (via `bmad-workflow-builder`, one commit): `tools/cost-manifest.py`
(two-mode instrument; the shared measurement substrate is its factored, marked layer) +
`tools/test-cost-manifest.py` (7/7 green) + the measure-first gate artifact
`skills/reports/cost-baseline-2026-07-29.md` (module mode at HEAD + vault mode against
vlt-core, read-only-proven, 4 figures hand-verified). Four numbered deviations recorded in
the brief's `status:` — notably vlt-core's structure map now carries every canonical key
(no live fallbacks; the fixture exercises that path) and the tolerant parser gained a
bracket-swallowed-header fallback (299/299 vlt-core entries parse). Ship-verifiable check 1's
evidence is on disk awaiting discharge; check 2 (work-vault run) remains the owner's.

**B5-2 BRIEFED 2026-07-29** (`skills/reports/build-B5-2-collision-debt.md`; re-ground clean —
HEAD `c1a4f9b`, B5-1 touched `tools/` only, every A5-16 site HOLDS, zero grounding
corrections; the designated second-pass-shape question ruled in the brief: callout-seeded
pair pass after the cluster pass, cluster formation untouched; its two acceptance checks are
in the ledger above, check 2 carrying the inherited A4-4 clause (5) debt's discharge).

**B5-3 BRIEFED 2026-07-29** (`skills/reports/build-B5-3-exact-facts.md`; re-ground clean —
HEAD `2c3740b`, every A5-8/A5-3 site HOLDS with B5-2's trivial line shifts, zero grounding
corrections; the designated slot question ruled (b) in the brief: define the check, tier-1 —
which entails `write-verification` 1→2 with a four-consumer re-ack; the brief also settles
the four-build strict-YAML fence PARTIAL, deletes the write-only `sources_count`, disqualifies
family stems (≥4 pages) as a near-dup signal, adds the `stubSlugs` exclusion channel, and
writes the extended proxy-check rule into the contract per cross-filing ruling 3 — B5-4 and
B5-9's briefs cite that paragraph, never re-word it; its two acceptance checks are in the
ledger above).

**B5-4 BRIEFED 2026-07-29** (`skills/reports/build-B5-4-spec-loop.md`; re-ground clean —
HEAD `2f19251`, every A5-6/A5-9/A5-15 capture site HOLDS (B5-2/B5-3's trivial vlt-lint line
shifts :86→:89, :81→:84), zero grounding corrections; two grounding additions inside the
designated questions' scope: vlt-lint's Step-5 template carries no `spec_candidate:` report
key at all, and the stamp's landing lock-out has an upgrade half — vlt-upgrade Step 2
refreshes bases wholesale, so a stamp would be clobbered to `null` on every upgrade. The
designated questions ruled in the brief: A5-6's three directions ALL adopted (acceptance beat
per cross-filing ruling 2's module instance; minimal declared-since age at the upgrade
reporter; deferral honesty repair) — the promotion step is the rule change making spec 1→2
real with the four-consumer re-ack; A5-15 ruled second-writer, as a general
ceremony-that-produces-the-first-instance authority rule (mint + promotion step + retrofit +
consult record), with the stamp landing in the base frontmatter and the divergence machinery
made adoption-aware (lint + upgrade compares exclude the line; upgrade apply carries the
stamp forward). A5-9's heading leg dies at both sites, replaced by a dated-revision-record
signal (git rejected — not required by conventions); the addendum lands as the
`consult_retroactive` third state (consult.md and frontmatter.md prose-clarified, no bump,
per the grouping's one-bump sizing). Its two acceptance checks are in the ledger above.)

**B5-3 BUILT 2026-07-29** (via `bmad-workflow-builder`; F1–F8 as briefed — the scanners now
extract verbatim facts (raw wikilink text, summary/verified_at/title values, index H2 list)
and JS computes every derived value (link normalization via the single-home `normalizeTarget`,
summary length verdicts, category↔H2 exact match, attestation presence/freshness);
`sources_count` deleted; family stems (≥4 pages) disqualified as a near-dup secondary signal;
`stubSlugs` third exclusion channel wired SKILL→workflow; the `sources_vs_prose_mismatches`
slot got its scoped-mode producer — `write-verification` bumped 1→2 with the wiki-page tier-1
item, all four consumers re-acked `@2` (package-lint Group E is the check of record), and
`vlt-lint` Step 2 gained the pointing check; the Step-5 fence now parses whole under strict
YAML, retiring the A4-2/A4-3/A4-4/A4-5 PARTIAL lineage — the fence turned out to carry two
MORE pre-existing breaks beyond the known one, both fixed (brief-status deviation 1); the
extended proxy-check rule landed in the contract's *Honest reporting* for B5-4/B5-9 to cite.
Verified at rest per the brief: positive + negative fixture runs through real Workflow
invocations, node --check, package-lint, single-home greps. 4 numbered deviations in the
brief's status.)

**B5-4 BUILT 2026-07-29** (via `bmad-workflow-builder`; F1–F7 as briefed — `spec.md` bumped
1→2 carrying the *Promotion from candidate* section (named-owner derivation, promote/decline
terminal states, honored-decline exclusion) and the deferral blind-spot statement, with all
four consumers re-acked `spec@2` (package-lint Group E is the check of record, bipartite
PASS); `vlt-lint`'s spec-candidate check re-cut to the dated-revision-record signal (bare
"What changed" headings are named non-evidence per the contract's extended proxy-check rule,
cited not re-worded), with the named-owner routing pointer, the Step-4 item shape + decline
guard, and the new `spec_candidate:` + `consult_retroactive:` report keys (whole-fence
strict-YAML PASS preserved); the consult precedence comparison landed beside pair-presence
with `consult.md`'s Enforcement prose sharpened to name both derived states (frontmatter
byte-unchanged, no bump); `vlt-upgrade`'s retrofit signal re-cut in step + the
first-instance stamp beat; the stamp authority rule
(ceremony-that-produces-the-first-instance: mint, promotion step, retrofit, consult record;
lint never) single-homed at `vlt-mint` Step 4, `frontmatter.md:242` made descriptive (no
bump), `vlt-dispatch`'s consult-block stamp beat added, the `adoption_first_instance:`
exclusion landed in both divergence compares (lint `:84` + upgrade pre-flight `:36`) and the
apply carry-forward in upgrade Step 2; the adoption null now reports its declared-since age
from the convention's own `created:`. Verified at rest per the brief: package-lint A/B/C/E
PASS, whole-fence `yaml.safe_load` PASS with the two new keys, two-site heading grep clean,
single-home greps clean, temp-fixture divergence-exclusion walk PASS (stamp-only diff clean;
a genuine edit still flags). 2 numbered deviations in the brief's status.)

**B5-5 BRIEFED 2026-07-29** (`skills/reports/build-B5-5-preserve-and-report.md`; re-ground
clean against HEAD `2f19251` + B5-4's uncommitted working-tree edits — every A5-2 and A5-17
capture site HOLDS, zero grounding corrections, two grounding additions inside the filings'
letter (Verify's own "minted-partner dir" clause as a sixth name-scoped site; Step 3.1's
under-claiming confirm clause). The designated questions ruled in the brief: A5-2's
`mints_preserved:` ruled **broaden, never add** (one provenance class, one field; key name
kept); A5-17 ruled **Verify-side field-completeness check** as the mechanism with a one-line
template required-lines annotation as belt, and the transient-report sub-question ruled no
(one net, anchored on the durable ledger). `vlt-mint:168` deliberately unedited — the promise
becomes true in the same release. Its two acceptance checks are in the ledger above, check 2
naming the work-machine vlt-sayari vault the factory cannot read.)

**B5-5 BUILT 2026-07-29** (via `bmad-workflow-builder`; F1–F6 as briefed, all in
`vlt-upgrade/SKILL.md` — the Step-1 snapshot bullet retitled **Local mints** and widened
from the `vlt-agent-*` name test to the provenance test B1 already uses (per-kind shipped
definition: `agents[]` for partners with the pristine-source caveat unchanged, presence in
the incoming bundle's skills for any other `vlt-*` dir; the bracket-path fallback now covers
both kinds via the live registry's shipped rows), with the Overview reframed to name both
kinds; the Step-2 own-the-apply prohibition widened to "unshipped local-mint `vlt-*` dir (a
minted partner **or** a vault-grown operation skill — the Step-1 provenance test)"; Step 3.1's
confirm and Step 3.2's B2 restore widened to local mints; the `mints_preserved:` /
`bodies_restored:` placeholders and the ledger Mints-preserved comment broadened with key
names and line labels kept (disposition 1 — broaden, never add); Verify widened to
local-mint dirs and extended with the nine-line ledger field-completeness check (disposition
2 — re-read the appended block, every Step-5 template line present, a missing line is a
defect to repair before closing), with the one-sentence required-lines annotation directly
after the Step-5 template as the belt. `vlt-mint:168` and `merge-help-csv.py` deliberately
unedited per the out-of-scope dispositions — the promise becomes true in this same release,
and B1 was the half that was always right. Verified at rest per the brief: widening grep
clean with only genuinely partner-specific survivors; the Verify enumerated list matches the
Step-5 template one-to-one; dry-read coherent Step 1 → Verify; B1 durability fixture run
PASS (`local_mints_preserved` lists both the `vlt-agent-x` partner and the `vlt-fizzbin` op
skill, the zombie row dropped); `vlt-mint:168` confirmed true under the widened Step 1;
package-lint A/B/C/E PASS; scrub clean. 3 numbered deviations in the brief's status
(Overview `:17` prohibition restatement widened as a seventh name-scoped surface; frontmatter
description left unchanged; no commit — release choreography rides the arc level).)

**B5-6 BRIEFED 2026-07-29** (`skills/reports/build-B5-6-decision-log-convention.md`;
re-ground clean against HEAD `2f19251` + B5-4's and B5-5's uncommitted working-tree edits —
every A5-4 capture site HOLDS (`vlt-mint:68-84` schema home + `:84` Q21 debt clause,
`vlt-lint` decision-log grep-zero with the governance findings at `:82-87`, `vlt-upgrade`
`:77`/`:85`, contract `:254-262`), zero grounding corrections, four grounding additions
(the `vlt-setup:144` enumerated copy list, the seed template as a second schema home to
shrink, the stamp authority rule bounding the lint write-through, the `.decision-log.md`
copy-exclusion non-collision). The promotion ruling (b) implemented: NEW
`{conventions}/decision-log.md` @1 (entry schema + required `ref:` key + two-tier
classifiability tail + supersession idiom citing `wiki-supersession.md`'s principle;
enforcement `checked`/`vlt-lint`/lint-run) with consumers `[vlt-mint, vlt-upgrade,
vlt-lint]` acked in the same build; `vlt-mint:68-84` and the seed template re-cut to
pointers; `vlt-lint` read-before-flag (three states) + write-through +
`governance_memory:`/`rulings_recorded:` keys; `vlt-upgrade` reconcile extended with the
filing's §5 migration leg. Five brief-time dispositions — notably **Q21 DISCHARGED** as
one-principle-three-applications (no merged super-convention; the `vlt-mint:84` debt
clause retires) and the lint write-through never stamps adoption. Its two acceptance
checks are in the ledger above.)

**B5-6 BUILT 2026-07-29** (via `bmad-workflow-builder`; F1–F6 landed as briefed. NEW
`{conventions}/decision-log.md` at `version: 1` — the entry schema with the required
`ref:` machine key, the two-tier classifiability tail (no `kind:` = pre-schema,
unclassifiable at all; `kind:` without `ref:` = scopable by class, unaddressable by
subject; append-only means no backfill, both tiers surfaced never swept), the supersession
idiom stated as the **third application of the never-silent principle** citing
`wiki-supersession.md` (the Q21 discharge — one principle, three bespoke applications; the
`vlt-mint:84` debt clause retired), the writers/readers roster, the honest limit
(recorded-only memory, the contract's derive-first boundary clause), and enforcement
`checked`/`vlt-lint`/lint-run with a derived-never-stored counter, no deferral block,
`adoption_first_instance: null`. `vlt-setup:144`'s brace list gains `decision-log` (the
A4-5 F2 precedent). `vlt-mint:68-84` re-cut to the pointer section *The mint decision log*
— schema fence, `kind:`-scoping restatement, and supersession mechanics deleted; the
mint-operational write moments kept; the ack `decision-log@1` added. The seed template
shrunk to a pointer (worked example dropped — the brief's named cheaper cut). `vlt-lint`
gains its ack, the **read-before-flag** preface over the long-lived governance findings
(exactly one of `adjudicated`-with-citation / `undisposed` / `unclassifiable` per finding;
the check's own bound stated; never-auto-fix untouched), the Step-3 **write-through** beat
(pointer-only, records a human's ruling, never stamps adoption), the `governance_memory:`
+ `rulings_recorded:` report keys, and their compose-yourself paragraph. `vlt-upgrade`
gains its ack, the reconcile pass's **second scan leg** (each current overlay, Step-1 base
divergence, and retired capability with no live `ref:`-keyed entry surfaces one
`undisposed` line for the human to rule once — idempotent; the honesty bound extended to
the two-tier tail), and the `:85` shape pointer re-targeted at the convention. Verified at
rest per the brief: package-lint A/B/C/E PASS with **Group E bipartite positively
confirmed** (decision-log v1 ↔ all three acks); single-home greps clean (the schema fence
lives in exactly one shipped file; both skill pointers target
`{conventions}/decision-log.md`); the Q21-retirement grep returns zero; whole-fence
`yaml.safe_load` PASS with both new keys, convention frontmatter strict-YAML flat-key
clean; the temp-fixture walk PASS (adjudication cites the live superseding entry — the
superseded twin is no match target; 1 pre-schema + 1 pre-key counted `unclassifiable`; the
undisposed overlay surfaces exactly once and the second walk skips it); an rsync demo
confirmed the dotted `.decision-log.md` copy-exclusion does not catch the undotted
convention basename; durability re-read clean (the new leg only surfaces; Step 3.7 writes,
human-gated); scrub clean; no `.decision-log.md` cruft; no commit. 3 numbered deviations
in the brief's status (fence placeholder de-bracketed for strict YAML; step anchors added
to the F3 pointer; the template example dropped, not updated).)

**B5-7 BRIEFED 2026-07-30** (`skills/reports/build-B5-7-boot-diet.md`; re-ground clean
against HEAD `2f19251` + B5-4..B5-6's uncommitted working-tree edits — every A5-10/A5-11/
A5-12 capture site HOLDS (partner openers `:22`/`:25`/`:31` ×3; contract ritual `:161-175`,
thread `:188`, consult seam `:213`/`:228`/`:230`; `vlt-consult.js:123-145`; dispatch
`:18`/`:202-206`; `consult.md` v1), zero grounding corrections; one grounding note (the
contract grew 38,271 → 38,958 bytes via B5-3/B5-4 — whale claim strengthened) and three
grounding additions (`tools/cost-manifest.py:244-291` hardcodes the pre-diet fixed-boot
composition and must follow the declaration; `vlt-setup:144`'s enumerated copy list must
gain the new asset; no skill outside the three partners eagerly reads the contract). Brief
opened under the POST-HOC-DEFAULT ruling as its recorded moment, mechanisms chosen against
the landed numbers (vlt-core baseline + vlt-sayari run): A5-10 ruled **(a) shipped
rule-card digest** (`_meta/vault-rule-card.md`, ≤8,000 bytes, content = the ruling's
identity-bearing + act-blocking criterion + a section map; factory-authored, marked
`derived_from:` with a new deterministic package-lint freshness check; (b) TOC rejected as
enumeration-shaped, (c) foreclosed by the ruling); A5-11 ruled **(c) both legs** (the
already-fresh skip widened to the ceremony read at both ritual homes; the consult-lite
boot stated in the engine prompt with dispatch §consult as mechanics home) and the
designated **bump-or-no-bump ruled NO BUMP** (`consult.md` gains one descriptive sentence
— prose clarification, `version: 1` kept, no re-ack); A5-12 ruled **(a) bounds from the
measured numbers** (`{log}` last-5 — the 4.7K/7.3K slice vs 151K/340K full — `{index}`
headings-tier, `{backlog}` open items, `## Thread` only as the one free (d) instance),
with (b) declined and (d)'s growth machinery + identity-read bounding named for B5-9's
vitals. Its two acceptance checks are in the ledger above.)

**B5-7 BUILT 2026-07-30** (implemented directly per the brief; F1–F8 + registration
landed. NEW `_meta/vault-rule-card.md` (5,511 bytes ≤ the 8,000 budget — frontmatter
`derived_from:` carrying the shipped contract's SHA-256 + derivation date; the
derived-artifact marker with the honest-limit line — derives from the *shipped*
contract, a vault-edited live copy is `governance_divergence`'s to surface; the becoming
frame; six act-blocking rule rows per the brief's letter, each a pointer at its contract
section, no mechanics restated; the 19-row H2 section map for point-of-use reads). The
three partner openers re-pointed at the card **byte-identically ×3** (the "read is
reinforcement" concession dropped) with the contract's `:161` ritual section moved in
step; Beat-2 bounds stated in both homes (`{index}` headings-tier, open sections on
demand; `{log}` last-5; `{backlog}` open items; `## Thread` only, `## Set aside`
excluded; dispatch slice + capabilities unchanged) plus the contract-home rationale
clause with the measured figures; the already-fresh skip widened to the rule-card read
at both homes (Beat 1 stays outside — the incoming partner still becomes itself).
Consult-lite boot at the three sites along the pointer chain: the engine `consultPrompt`
exemption sentence (LITE BOOT — no activation ritual, no rule-card/contract read, no
Beat-2 orient; depth-1 traffic, not a sitting), the dispatch §consult mechanics sentence
(entry lite; exit was already exempt per the no-sitting-boundary rule), the `consult.md`
descriptive sentence — `version: 1` kept, **no re-ack, Group E the check of record**.
`cost-manifest` module mode recomposed: fixed boot = SKILL + rule-card; the contract
wholly in lazy governance stock (broken-out table + named-aggregate row kept); comment/
prose homes updated; vault-mode last-5 slice relabeled "the declared Beat-2 bound".
`package-lint` gains **C6** (derived-artifact freshness: card exists, `derived_from:`
sha256 equals the shipped contract's actual hash, size ≤ 8,000) homed inside Group C so
the release summary line format holds. `vlt-setup:144`'s copy list carries the card with
the module-owned overwrite posture stated (distinct from the skip-if-present class);
`vlt-upgrade:48` **verified, not edited** — own-the-apply refreshes `_meta/` wholesale,
the new asset rides. Verified at rest per the brief: package-lint A/B/C/E PASS **and** a
one-byte contract perturbation in a temp copy FAILs C6 (the check can fail);
test-cost-manifest 7/7 green; `node --check` clean; per-partner fixed boot now
15,896–17,324 bytes vs the baseline's 48,340–49,768 rows (**~64–67% cut**, ≥60% the
bound); two-home agreement greps clean (partner lines 22/31 byte-identical ×3; surviving
`{contract}` mentions are point-of-use pointers + the librarian's init existence check);
scrub clean; no `.decision-log.md`; no commit — release choreography rides the arc
level. 4 numbered deviations/notes in the brief's status (contract intro `:17` also
re-pointed — it restated the openers' reinforcement concession and would have gone
stale; F8 homed as C6 per the granted builder's-choice; the harness case-1 slice-label
assertion updated with F7's relabel; upgrade verified-not-edited).)

**B5-8 BRIEFED 2026-07-30** (`skills/reports/build-B5-8-whale-recut.md`; re-ground clean
against HEAD `2f19251` + B5-4..B5-7's uncommitted working-tree edits — every A5-14 capture
site HOLDS with the whales strengthened (lint SKILL 41,202 → 49,025 bytes, dispatch 38,285
→ 38,843; single-home still holding; inbound pointers all mode/section-named, never
line-numbered), zero grounding corrections; four grounding additions (cost-manifest's
SKILL.md-only whale aggregates must follow the re-cut; package-lint E3's SKILL.md-only
scan is a coverage hole the re-cut would create; vlt-upgrade's wholesale `vlt-*` copy
carries reference files but its `reports/` exclusion constrains the dir name;
`vlt-lint:82`'s ack-coverage clause must name reference files). **The pinned
`package-lint.py` packaging-expectations check was performed before any re-cut was
scoped** — six findings recorded in the brief's own section, notably: references ship
automatically (C5 maps dirs, not files), E3 must widen to `references/*.md` in the same
build, `depends_on:` must stay in router frontmatter (E1 reads only there), and no check
yet verifies router↔reference agreement — the build adds the deterministic
router-integrity net (the B5-7 C6 precedent). Mechanisms chosen under the POST-HOC-DEFAULT
ruling (cited, not re-litigated): A5-14 ruled **(a) adopted for both skill whales** —
dispatch cut per mode (four `references/` files; router keeps identity, routing,
act-blocking rules, and the pickup loop; measured savings ~45–60% per single-mode
invocation), lint cut per step (`full-scale.md`/`checks.md`/`fix-and-file.md`/`report.md`;
the honest bound stated — full runs conserve total load, savings concentrate in scoped
runs, early exits, and the trigger surface); **(c) discharged by B5-7** (the rule-card is
the contract's progressive disclosure; no contract byte this build); **(d) satisfied**
(cut lines from the measured section sizes); **(b) composable inside the move** (the
single-home greps are the check). Budgets set as acceptance bounds (dispatch router
≤ 14,000 bytes, lint ≤ 12,000, per-skill total ≤ 105% of pre-cut); **no convention bump
anywhere** — Group E must confirm by machine; `frontmatter.md` declined with reasons
(lazy surface; a split would demand frontmatter@5 + five-consumer re-ack — future filing
territory, recorded as a narrowing of B5-7's out-of-scope sketch). Its two acceptance
checks are in the ledger above.)

**B5-8 BUILT 2026-07-30** (implemented directly per the brief; F1–F5 landed, registration
none as briefed. **F1** — `vlt-dispatch` re-cut per mode: router SKILL.md **11,281 bytes**
(≤ 14,000) keeping the frontmatter whole (acks byte-identical), Overview, On Activation
with the act-blocking rules verbatim (human-zone boundary, secret hygiene,
single-writer), Mode dispatch now naming each mode's reference as the read-on-entry
("every mode reads its reference on entry", `ledger` included — no exceptions list), the
pickup loop in full, and a short per-mode Log preamble; NEW `references/daily.md`
(11,007) / `relay.md` (7,031) / `consult.md` (9,126) / `ledger.md` (1,811) — each mode
body verbatim plus its `{log}` line shape and its Verify checklist (the consult one-tag
log rule rides with consult); per-skill total **40,256 ≤ 40,785** (105% of 38,843).
**F2** — `vlt-lint` re-cut per step: router **6,261 bytes** (≤ 12,000) keeping the
frontmatter whole (all 8 acks byte-identical), Overview, On Activation incl. the
JIT-read/overlay rule, Step 0 scope with the early nothing-in-scope exit router-only plus
a `full-scale.md` pointer, Step 1, a six-line step-sequence map, the act-blocking
Standing rules (never-auto-fix tier-2/governance, the never-auto-apply list,
single-writer-in-the-SKILL, never-stamp-adoption, write-through-records-a-human's-ruling-
only), and Step 6's log line; NEW `references/full-scale.md` (3,611) / `checks.md`
(25,023) / `fix-and-file.md` (6,243) / `report.md` (10,306 — the Step-5 fence
strict-YAML-parses whole at its new home, B5-3 not regressed); total **51,444 ≤ 51,476**
(105% of 49,025). **F3** — `package-lint.py` gains **C7** (router integrity, both
directions: orphan reference and dangling `references/<name>.md` route each FAIL; plus
the two router byte budgets as `ROUTER_BUDGETS` beside `RULE_CARD_BUDGET`, homed in
Group C so the `A/B/C/E PASS` summary format holds) and **E3 widened** to
`skills/vlt-*/SKILL.md` + `skills/vlt-*/references/*.md` (deliberately not a blanket
glob — `vlt-setup/assets/**` keeps its own jurisdiction). **F4** — the ack-coverage
clause now reads "workflow assets and reference files (… and its `references/`)", landed
in `references/checks.md` riding the F2 move; no convention edited, nothing bumps.
**F5** — `cost-manifest.py` whale aggregates dir-aware: `lint surface (SKILL.md +
references/)` 51,444 + `lint router (SKILL.md)` 6,261; `dispatch surface` 40,256 +
`dispatch router` 11,281; the SKILL.md-only section keeps its label and gains the
one-sentence references note; the harness truth table follows (dir-aware walk). Verified
at rest per the brief: package-lint A/B/C/E PASS on the tree **and** three temp-copy
negatives each FAIL correctly (deleted reference → dangling-route C; planted
`frontmatter@4` in a reference → widened-E3 E; stray unrouted reference → orphan C);
test-cost-manifest 7/7 green; both routers' frontmatter blocks byte-identical to pre-cut
and zero convention files touched — **the no-bump is machine-confirmed, not asserted**;
single-home greps clean both directions (watermark mechanics / relay idempotency pair /
engine invocation block / typed return union / Step-2 bodies / Step-4 item shapes /
Step-5 fence each in exactly one file; surviving router mentions are rule-statements +
pointers); inbound-pointer walk — all ten Re-grounding sites resolve through surviving
mode/step names, **zero inbound edits**; `vlt-upgrade:48-49` **verified, not edited**
(the wholesale `vlt-*` copy carries `references/`, the exclusion list does not catch the
name, `skill_asset_divergence`'s manifest compare accommodates new shipped files);
dry-walks complete (a `consult` reads router + its reference only; a scoped lint never
opens `full-scale.md`; the nothing-in-scope exit reads the router alone); scrub clean; no
`.decision-log.md`; no commit — release choreography rides the arc level. **No
deviations** in the brief's status.)

**B5-9 BRIEFED 2026-07-30** (`skills/reports/build-B5-9-enforcement-kit.md`; the pinned
**091003 M0 + parked Arc-3 rulings re-read discharged before scoping** — the filing re-read
in full, the Arc-3 archive's build-17 remit ruling (unchanged minus the two re-homed
pieces), M0 disposition, and decide-once rulings (code home `{root}/.claude/hooks/`;
lint/dispatch find, tripwires nag; hardcoded dispatch read) all re-read and cited; M0 stays
unpayable until this build ships and becomes acceptance check 2's named field event.
Re-ground clean against HEAD `2f19251` + B5-4..B5-8's uncommitted edits — every
A5-19/A5-7/A5-18 site HOLDS in substance, **zero grounding corrections**; the capture's
pre-recut addresses (`vlt-lint:62`/`:104`, dispatch `:297`) re-home to
`vlt-lint/references/checks.md:17`/`:64` and `vlt-dispatch/references/ledger.md` per the
documented B5-8 re-cut. Five grounding additions inside the rulings' letter (the marked
substrate seam at `cost-manifest.py:36-49`; B5-7's two deferrals as size vitals; the
C6/C7 deterministic-check precedent → new C8; frontmatter@4's five consumers + vlt-dispatch
joining as a sixth; vlt-upgrade verified-not-edited via its Step-6 reconfigure hand-off).
The designated questions ruled in the brief: **A5-18's persistence home** = dated artifact
at a new `lint_reports: _agent/lint-reports/` structure-map path, both modes persist,
append-only, session-note option rejected (lint is forbidden session notes);
**`days_since_lint`** = display-only (filing's own recommendation; two seed wires only);
**`blocked_on`** = inline greppable facet with `external-event` requiring a companion date,
riding **frontmatter 4→5** with a six-consumer walk (vlt-dispatch joins). A5-7 shape 3
lands as the derived, denominated, display-only classifier-streak line (shape 2 cited
NOT-BLOCKING per the roadmap disposition); the two wire forms reconciled — frontmatter
deferrals are the per-convention wires, the registry holds vault-level wires, one finder
per fact; the vitals reader is stdlib-Python extending B5-1's substrate re-homed to a
shipped asset (`assets/hooks/vlt-vitals.py`, cost-manifest imports it — the
`cost-manifest.py:40` open note resolves); POST-HOC DEFAULT cited, not re-litigated. Its
two acceptance checks are in the ledger above.)

**B5-9 BUILT 2026-07-30** (implemented directly per the brief; F1–F10 + registration
landed. **F1** — NEW `skills/vlt-setup/assets/hooks/vlt-vitals.py` (stdlib-only, ~24K):
the shared measurement substrate re-homed (tolerant `{log}` parser + structure-map
resolver moved in verbatim-in-behavior, the resolver now taking an injectable default
map with embedded shipped defaults for the installed-vault case), the canonical METRICS
table (10 ids: `ingests_since_lint` op-debt-pinned, `days_since_lint`, `open_pointers`,
`oldest_open_pointer_days`, `expired_pages`, `classifier_streak` denominated with the
two-tier K bucket, four size vitals), registry evaluation (absent registry = plain
denominated zero; unknown metric id = loud per-wire error), two modes (full report with
the derive-only banner; `--strip` ≤1 line, silent when green, `vitals unavailable
(<reason>)` + exit 1 on reader failure); `tools/cost-manifest.py` now IMPORTS the
substrate (banner rewritten — the A5-19 Q1 note resolved; duplicated parser block
deleted; `sys.dont_write_bytecode` guards the import), harness 7/7 green. **F2** — NEW
`assets/tripwires.yaml` seed beside module-help.csv (schema-in-header per disposition
10: seven required fields, derive-first rationale, canonical-vocabulary pointer,
write-moments rule, merge posture) carrying exactly the two stock wires (`lint-debt`
ingests_since_lint ≥ 10; `relay-overdue` oldest_open_pointer_days > 21). **F3** —
`module.yaml` default map + the contract's semantic table gain `tripwires` +
`lint_reports` rows (E2 agreement held). **F4** — `vlt-setup` §2b: hook force-reinstall
to `{root}/.claude/hooks/` (Arc-3 code-home ruling cited), seed skip-if-present with
merge-by-id (local thresholds win, local wires never dropped), idempotent
`settings.json` SessionStart registration keyed on the script path never touching
unrelated hooks, `{lint_reports}` created-if-absent; Confirm report bullet extended;
`vlt-upgrade` **verified, not edited** (Step 6 reconfigure hand-off runs §2b on every
upgrade). **F5** — the contract's `{log}` section is now a **declared machine-read
grammar** (parsers case-insensitive + paren-tolerant of history; paren mandatory for
partner-run operations going forward, history grandfathered); rule-card `derived_from:`
SHA re-stamped in step (stale-SHA C6 FAIL observed first, then PASS). **F6** — lint:
description's "proactively after several ingestions" now names `lint-debt` as its
counter; Step-0 two-definitions pin (mtime scoping vs op-debt wire, co-legitimate);
Step-6 counter-reset-by-derivation line + the persist beat (Step-5 block verbatim to
`{lint_reports}/YYYY-MM-DD-HHMM-lint.md`, both modes, append-only — single-writer holds);
`checks.md:17`/`:64` promises now live pointers at `{tripwires}` + the ledger surface;
meta-check gains `counter_unknown_metric` (find, never nag); `report.md` fence gains the
key, strict-YAML whole-parse preserved. **F7** — `ledger.md` gains "Tripped wires &
vitals" (tripped rows with owner + surface_text; reader failure = warning row, never
faked green; denominated zero "0 of 2 wires tripped" per *Honest reporting*, cited;
display-only vitals block; `blocked:` facet grouping with untagged bucket) + the Verify
wires-match-a-fresh-run line; router ledger blurb gains "plus tripped wires". **F8** —
`frontmatter.md` **4→5**: backlog tuple gains the optional
`blocked: user-decision | partner-bandwidth | external-event YYYY-MM-DD` facet
(triage-metadata-never-status; external-event requires its date; zero backfill); `:231`
re-worded to the landed one-vocabulary form; **six-consumer walk** — vlt-ingest /
vlt-extract / vlt-research / vlt-mint re-ack `@5` verified-no-edit, vlt-lint re-acks
carrying F6, **vlt-dispatch joins `consumers:` and acks `frontmatter@5` for the first
time** with daily/relay pointer-shape notes (the facet may ride the paren); Group E
bipartite PASS is the check of record. **F9** — the non-convention-deferral wire rule
landed at `vlt-mint` Step 2a capture and the council's Step 3 return (an "until it
bites" deferral MUST register a registry wire in the same human-gated moment; no op
ever writes counters, cited). **F10** — package-lint **C8** homed in Group C: seed
parses + wires complete, metric ids exist in the imported METRICS table (parsed, never
re-declared — WIRE_REQUIRED_FIELDS imported too), vlt-vitals.py compiles (temp-path
.pyc, no cruft), module.yaml carries both rows. Verified at rest per the brief:
package-lint A/B/C/E PASS with C8 live + three temp-copy negatives each FAIL correctly
(bogus metric / syntax-broken reader / deleted tripwires row); test-cost-manifest 7/7;
vitals fixture runs hand-verified both modes (12 ingests-since-lint, 29 days, 2 open /
oldest 30d, 1 of 3 expired, streak 2 of 3 with K=2 — independent greps agree; strip
trips both wires; green fixture silent exit 0; mangled `{log}` and non-vault render
`vitals unavailable` exit 1; bogus wire metric is loud); settings-merge demo run twice —
byte-identical second run, vault-local hook byte-identical, strip hook exactly once;
strict-YAML fence PASS with `counter_unknown_metric`; single-home greps clean (METRICS +
tolerant parser only in the asset; wire schema in the seed header with machine consumers
importing; `{lint_reports}` write only in the lint router); dry-walks complete; scrub
clean; no `.decision-log.md`, no `__pycache__`; no commit — release choreography rides
the arc level. 4 numbered deviations in the brief's status (settings idempotency key =
bare script path; C8 imports the wire field list + temp-path py_compile; harness
EXPECTED_FALLBACKS follows the two new canonical keys; missing-`{log}`-fails-loud /
absent-registry-plain-zero boundary ruled).)

**Next lifecycle move: release** — the arc's planned builds are all BUILT; the release
rides `vlt-release` as its own gated sequence (B5-4..B5-9 UNCOMMITTED awaiting it; dual
version bump + `--expect-version` lint gate per the release contract). Historical:
**build B5-9** (a fresh builder session implements
`build-B5-9-enforcement-kit.md` via `bmad-workflow-builder`; builder exit obligations:
rewrite the brief `status:` to a BUILT record with numbered deviations, delete any
`.decision-log.md`, one commit for the build). After it: the arc release rides
`vlt-release` (B5-4..B5-8 remain UNCOMMITTED awaiting it; B5-9 is the last planned
build). Historical: **brief B5-9** (the enforcement kit, riding last per the ideation
ship order — carrying B5-7's named deferrals: `{log}` rollover/size-bell growth machinery
and the identity/capabilities pruning-vitals question). Historical: **build B5-8** (a fresh builder session implements
`build-B5-8-whale-recut.md` via `bmad-workflow-builder`), then **brief B5-9 last**. Historical: **brief B5-8** — the whale re-cut (A5-14; its own pinned
`package-lint.py` pre-brief check per B5-7's out-of-scope note), then **B5-9 rides
last** (the enforcement kit's seam, per the ideation ship order — now also carrying
B5-7's named deferrals: `{log}` rollover/size-bell growth machinery and the
identity/capabilities pruning-vitals question). Historical: **brief B5-7 was PAUSED at the measurement
gate, owner-confirmed 2026-07-30.** Offered the alternatives (open B5-9 early; proxy on the vlt-core baseline;
release B5-4..B5-6 first), the owner ruled **pause here**: the work-vault instrument run
happens first (the owner's), then the pre-hoc/post-hoc ruling against those numbers, then
B5-7 opens. **UPDATE 2026-07-30: the work-vault numbers LANDED** — the owner directed the
run against `vlt-sayari` (readable from the factory machine; the cannot-read premise no
longer holds), filed as
`inbox/2026-07-30-111133-b5-1-work-vault-cost-manifest-run.md` (read-only-proven,
spot-verified against `wc -c`). **UPDATE 2026-07-30, same day: the pre-hoc/post-hoc
ruling is MADE — POST-HOC DEFAULT** (full record in the cross-filing rulings section
above; briefs cite it there, never re-word). **The measurement gate opened fully;
B5-7 was then briefed and built (records above).** Historical note — **B5-7 and B5-8 were gated** on the B5-1
measurement numbers (the work-vault instrument run is still outstanding — the owner's)
and on the owner's deferred **pre-hoc/post-hoc governance ruling**, which is the first
act of boot-cost briefing (pre-ideation ruling 2); **B5-9 rides last** (the enforcement
kit's seam, per the ideation ship order). Each brief tags every acceptance check
**ship-verifiable** or **field-contingent** — only ship-verifiable checks gate
`arc-closeout`. The two factory
process records (A5-1, A5-5 residue + the factory half of the named-slot principle) are
owner/clerk work outside the build plan; nothing in this roadmap's ledger will wait on
them.
