# Field inspection — vlt-core (raw findings)

Handoff dossier section 03. Read-only inspection of the primary field vault at
`{field-vault}`, 2026-07-12, compared against module source at
`{factory-root}` (repo HEAD = a117f4f, v0.6.0).
Nothing in the vault was written, moved, or deleted.

---

## 1. Layout and installed version

Vault top level: `.claude/`, `.git/`, `.obsidian/`, `_agent/`, `_archive/`, `_bmad/`,
`_meta/`, `_vault/`, PARA dirs (`areas/`, `projects/`, `resources/`, `sources/`,
`daily/`, `new/`), `CLAUDE.md` (2.3K, vault-written orientation: "This vault is tended
by the **Vault** BMad module — a cast of partners…").

Install surfaces:
- **Skills:** `.claude/skills/` — 14 shipped `vlt-*` skills + 3 minted local partners
  (`vlt-agent-chef`, `vlt-agent-dog-trainer`, `vlt-agent-health-coach`) + 3 shipped
  partners (`vlt-agent-librarian`, `vlt-agent-researcher`, `vlt-agent-creative`),
  alongside ~60 `bmad-*` skills from core/bmm/bmb/cis.
- **Workflows:** `.claude/workflows/vlt-lint-full.js` (20.2K), `vlt-review-council.js` (12.1K).
- **BMad config:** `_bmad/vlt/config.yaml` (generated by installer 6.9.0,
  2026-07-03) + `_bmad/config.yaml` + `_bmad/module-help.csv` (18 lines = header + 17
  rows, all `Vault` module rows; canonical header confirmed post build-13 migration).
- **Live governance:** `_meta/` — `vault-operating-contract.md` (31.8K), 5 personas,
  7 conventions (see §4).
- **Agent zone:** `_agent/` — 369 files. This is where all vault growth lives.

**Version: 0.6.0 — the vault is ALREADY on the current release.**
- `_bmad/config.yaml` line 8: `version: 0.6.0`.
- `.claude/skills/vlt-setup/assets/module.yaml`: `module_version: 0.6.0`.
- `_agent/upgrade-ledger.md` top entry: `## [2026-07-08 17:45] vlt 0.5.0 → 0.6.0 (own)`
  — own-the-apply from the factory repo, i.e. the 0.6.0 acceptance run has already
  ridden (the roadmap/memory note "acceptance pending" is stale as of 07-08).
- **Stale counter-signal:** `_bmad/_config/manifest.yaml` still reads
  `name: vlt / version: 0.4.0 / lastUpdated: 2026-07-03T13:00:26` — the installer-side
  manifest is NOT bumped by own-the-apply upgrades. Two version registries exist and
  only `_bmad/config.yaml` is maintained. Anything (installer, bmad-help) reading
  `_config/manifest.yaml` sees 0.4.0.

## 2. Drift vs module source: effectively zero

`diff -rq` of repo `skills/` vs vault `.claude/skills/` (excluding `reports/`,
`__pycache__`, `.DS_Store`, vault-only dirs) returned **no differences** — every
installed shipped file is byte-identical to repo HEAD. No hand-edits to module-owned
files. The only intentional vault-side residue in a skill dir is
`.claude/skills/vlt-mint/.decision-log.md` — the A1 relocation stub
(`# MOVED — see \`_agent/mint/decision-log.md\``), preserved by design across four
upgrades.

Upgrade history (`_agent/upgrade-ledger.md`, append-only, 5 entries): 0.2.0→0.3.0
(2026-06-24), 0.3.0→0.3.1 (2026-06-24), 0.3.1→0.4.0 (2026-06-26), 0.4.0→0.5.0
(2026-07-03), 0.5.0→0.6.0 (2026-07-08). The 0.3.1 entry closed the vault's only-ever
governance divergence (dispatch-slice re-graft, upstreamed); every close since has
been "fully on stock, zero residual divergence." The 0.5.0 entry records the help-csv
header migration (`header_migrated: true`); the 0.6.0 entry records spec.md +
write-verification.md convention seeding, the new `_agent/specs/` structure entry, the
build-18 skill-asset manifest write, and the BMad cache seed refresh.

## 3. Vault-local growth (the durability surface for the next upgrade)

Everything below is vault-grown and MUST survive merge-not-replace:

- **3 minted partners:** `vlt-agent-chef`, `vlt-agent-dog-trainer`,
  `vlt-agent-health-coach` — skill dirs + 3 local rows in `_bmad/module-help.csv`
  (14 shipped + 3 local = 17, confirmed by every ledger entry).
- **Partner state:** `_agent/partners/{chef,creative,dog-trainer,health-coach,librarian,researcher}/`
  — all 6 on the two-file `identity.md`/`thread.md` layout. Thread recency: librarian
  Jul 11, researcher Jul 8, creative Jul 5, dog-trainer Jul 3, chef Jul 1,
  health-coach Jun 27. Librarian thread is the giant (47.7K thread, 20.9K identity).
- **Light capabilities (partner-owned):**
  `_agent/partners/creative/capabilities/{obsidian-bases.md,taste-interview.md}`
  (each with a subdir: authoring-reference/protocol/working-file for obsidian-bases),
  `_agent/partners/librarian/capabilities/ingest-youtube.md` (11.3K) +
  `capabilities/scripts/clean-captions.py` (4.1K — the 0.5.0 scripts-sibling shape,
  exercised in the field).
- **Mint history:** `_agent/mint/decision-log.md` (56.5K, entries through 2026-07-03
  — latest: the `_agent/training/` → `_agent/dog-training/` namespace migration) + 3
  planning docs (`2026-07-01-obsidian-bases.md`, `2026-07-01-taste-interview.md`,
  `youtube-ingest-brief.md`).
- **Convention overlays:** NONE — `_agent/conventions/` contains only `.baseline/`
  (7 baseline conventions + the build-18 `.skill-manifest`, 42 SHA-256 lines covering
  every shipped vlt skill file + workflows). Overlay machinery has never fired in the
  field.
- **Bases:** `_agent/bases/wiki.base` (1.6K) — one live Obsidian Base; it empirically
  resolved the undocumented `.base` `sort:` serialization (backlog item RESOLVED
  2026-07-08; the vault's Creative declined to invent the key, the owner set it via UI).
- **Config override:** `_bmad/config.yaml` `vault_structure.dog_training_root:
  _agent/dog-training/` — lives only in vault config, not module.yaml; the 0.5.0
  ledger explicitly flags it as the reason to never destructively re-materialize.
- **Loop-state roots:** `_agent/dog-training/penny/{baseline-intake.md,progress.md
  (24.2K, through Jul 3)}`, `_agent/health/{body-log.md,strength-log.md,program.md,
  exercise-library.md}` (through Jun 27).
- **Vault-local enforcement-kit prototype (build-17 evidence, NOT shipped):**
  `_agent/tripwires.yaml` (self-described "enforcement-kit slice 1, design-stage
  prototype"), `_agent/vitals.sh` (4.9K), `_agent/session-health-strip.sh` (2.9K).
  Derive-first design stated in the file header: "no wire reads stored state — every
  metric is recomputed on each check." Wires: `lint-debt` (>=10 ingests since lint),
  `relay-overdue` (>21-day open pointer) — the latter demonstrably fired and was
  cleared 2026-07-09 ("parked the two ideation dispatch items (cleared the
  relay-overdue tripwire)"). These files appear in module source only in dev briefs
  (`skills/reports/`), confirming vault-grown. **They sit at `_agent/` top level —
  the next upgrade's durability sweep must treat them as vault growth.**
- **Design system:** `_agent/design/{brand-system.md,brand-system-reference.html,
  warp/signal-{dark,light}.yaml}` — Creative-produced vault brand assets.
- **Evolution artifacts:** `_agent/artifacts/` — 15 files incl. six
  `problem-solution-2026-07-06*.md` docs (spec-convention, enforcement-kit,
  ingestion-ledger, no-boundary-without-a-bell, self-cleaning-writes) that became the
  Arc 3 inbox filings, `vault-evolution-synthesis-2026-07-06.md`,
  `vault-evolution-workflow.js` (a vault-local workflow!),
  `enforcement-kit-evidence.md` (5.7K, Jul 8), `problem-solution-2026-07-11.md` (32K)
  and `research-wiki-audit-2026-07-11.md`, plus a brainstorm dir with `.memlog.md`.

## 4. Governance state

Live `_meta/conventions/` == shipped bundle: extraction@2, frontmatter@3, spec@1,
wiki-consolidation@1, wiki-index@2, wiki-supersession@1, write-verification@1 — the
full 0.6.0 seven, matching `.baseline/` and repo source. Operating contract + 5
personas present. No local edits (ledger: "Governance divergence: none" at every
close since 0.3.1).

## 5. Evidence of real use (heavy, current, librarian-centric)

- **Operation log** `_agent/log.md`: 314 lines, **201 timestamped entries**, latest
  2026-07-11 15:10. Install-to-now span 2026-06-06 → 2026-07-11.
- **Sessions:** 82 files, 2026-06-06 → 2026-07-11 (~2.3/day). Recent run is
  ingest-heavy (three ingest sessions on Jul 11 alone).
- **Wiki:** 124 pages + `index.md` (17.7K, updated Jul 9). Full lint 2026-07-08:
  "checked 122 files … 28 unhandled + 48 handled contradictions … 28 pages fixed
  … all attested."
- **Research:** 90 notes (2026-04-11 migrated-era → 2026-07-11).
- **Dispatch** `_agent/dispatch.md` (38K): watermarked routing ledger, 63 checked
  items, **2 open** (incl. the 2026-07-09 librarian→researcher tax-provision relay).
- **Handoffs:** 7 files, 2026-06-13 → 2026-07-08, real cross-partner shape
  (`health-coach-to-chef`, `researcher-to-librarian`, …).
- **Backlog** `_agent/backlog.md` (42.4K, 57 lines): living — mixed resolved/open;
  open items incl. the warm-up-mechanism leak (dog-trainer content call), the
  ingested-sources ledger gap (explicitly marked "Module-feedback candidate", since
  filed as the 07-06 ingestion-ledger inbox filing), the Warp config-schema gap, and
  the 5 deferred new-page items from the 07-11 audit.
- **Sources:** 87 files under `sources/{articles,books,docs,metrics,notes,papers,
  recipes,transcripts}` — incl. the two 17–18k-word YouTube transcripts consumed by
  the `ingest-youtube` prep/interpret split on Jul 11 ("kept the transcript out of
  context").
- **Skills demonstrably exercised:** vlt-ingest (dominant), vlt-lint (scoped + full,
  attested), vlt-research, vlt-dispatch, vlt-mint (3 partners + 3 capabilities + a
  capability migration), vlt-track (loop roots live), vlt-extract (2 personalized
  extractions cited in ledger), vlt-review-council (mint decision-log records council
  verdicts), vlt-upgrade (5 ledger entries), vlt-setup (initial), vlt-query (session
  types include query-shaped work; weakest direct trace).

## 6. Shipped machinery with NO field artifacts

- **Capability families:** `_agent/capabilities/families/` contains only `.gitkeep`.
  Never exercised across the entire vault life ("families/ empty" in all 5 ledger
  entries). Shipped since 0.3.0, still zero field use.
- **Loop profiles (vlt-track, 0.4.0):** `grep -c "Loop profile"` across all 5 live
  capability files = 0. The Arc-1 watch item stands: loop-profile machinery unexercised
  (the health/dog-training loops predate it and never adopted the section).
- **Convention overlays:** never created (`*.overlay.md` count = 0). Overlay-lift and
  overlay-subsumption paths (build-18) have never run against real divergence.
- **`_agent/specs/`:** exists (created by the 0.6.0 upgrade) and is **empty** — the
  spec convention (build-15) has no field artifact yet. The 7 pre-spec handoffs are
  the precursor shape. The Arc-3 hard gate "next mint/handoff uses the shipped spec
  text" is still unmet in the field.
- **`review_after` freshness key (build-16):** adopted on exactly **1 of 124** wiki
  pages (`_agent/wiki/self-hosting.md`, the newest cluster). `verified_by`/
  `verified_at` attestation: **37 of 124** pages. Rollout is organic-on-touch, so
  coverage is expectedly partial — but 30% attested / <1% freshness is the current
  truth.
- **`_agent/artifacts/{planning,implementation}-artifacts/`:** both empty dirs
  (BMad output_folder scaffolding, unused).

## 7. Friction / defect evidence on disk

- **Stale installer manifest** (§1): `_bmad/_config/manifest.yaml` vlt at 0.4.0 while
  the vault runs 0.6.0. Candidate inbox filing: own-the-apply should also touch the
  installer manifest (or the discrepancy should be documented as intended).
- **iCloud conflicted-copy cruft:** `_agent/sessions/2026-06-20-103600-misc
  (Conflicted copy iPhone 202606201349).md` still present. (The health-coach
  identity/thread conflicted copies flagged in three ledger entries HAVE been cleaned
  — partner dir now holds only the two canonical files.)
- **Root cruft:** `/Untitled.md` (3 bytes, content `935`) at vault root.
- **28 unhandled contradictions** left by the 2026-07-08 full lint (documented, not
  silently dropped): e.g. the tax-provision staleness cluster is parked on an open
  dispatch relay because the librarian "won't fabricate current law."
- **Recurring `</content>` residue saga** (backlog, RESOLVED 2026-07-03): stray
  source-format residue seeded 7+ wiki pages across two causes before the
  ingest-Step-9 + lint guards landed — the clearest historical example of the
  enforcement gap Arc 3 targets.
- **The enforcement-kit prototype at `_agent/` top level** (§3) is field-run design
  work for unbuilt build-17 — live evidence (thresholds, fired wires, vitals output
  shape) the build-17 brief said it was waiting for.
- **No orphaned/malformed artifacts found** beyond the above: no stray
  `.decision-log.md` (only the intentional stub), no `.DS_Store`-driven divergence in
  the skill diff, help-csv canonical and 17-row-consistent.

## 8. Handoff takeaways

1. Field == source at 0.6.0, byte-identical — there is no drift debt; the
   own-the-apply + ledger discipline demonstrably works (5 clean upgrades, one
   divergence ever, upstreamed and closed).
2. The durability surface is now larger than the merge scripts' historical checklist:
   top-level `_agent/{tripwires.yaml,vitals.sh,session-health-strip.sh}` and
   `_agent/artifacts/vault-evolution-workflow.js` are new vault-grown shapes.
3. Unused-machinery list for pruning-or-patience decisions: capability families
   (never), loop profiles (never), overlays (never needed), specs (gate pending),
   freshness key (1 page).
4. Stale installer manifest + conflicted-copy session file + root `Untitled.md` are
   the only cruft; all are one-line fixes but two are module-process questions
   (manifest ownership; whether upgrades should flag conflicted-copies).
