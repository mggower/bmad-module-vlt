# build-7-para — fixture tree for `briefs/build-7-roster-closure-retired.md` checks (3) and (4)

**Specimens: 0/27.** The parent filing (A15-13, `2026-09-01-170000`) observed **27** partner-written,
unattested Layer-3 files across six partner domains and preserved **counts only** — no path, no
frontmatter fragment (it is a supersession, not a defect specimen). The observed state (*unattested,
cannot be attested*) is the state the retirement makes escapable, not one the net judges, so a
pre-fix specimen could not exercise the post-fix join by construction. This tree is therefore authored
from the failure's **shape** as the filing states it — `author: agent|hybrid`, partner-written, Layer-3,
outside `{wiki}` — plus owner ruling D-E's cases (brief disposition 8). The low figure is the honest one.

Layout (`{projects}` → `projects/`, `{areas}` → `areas/`, `{resources}` → `resources/`, `{wiki}` →
`resources/wiki/`): **C1** `projects/alpha/` declares `writers: [human, librarian]` (flow form) with a
declaring-free `sub/`; **C2** `areas/beta/` declares `[human, agent]` (block form); **C3** `areas/gamma/`
declares `[human]`; **C4** `resources/delta/` declares nothing (open). The oracle is
`../build-7-expected-findings.json` (hand-written first); the instrument is `../build-7-reader-protocol.md`.
Placeholder names throughout; no vault content.
