---
topic: Cycle 13 build-1 — reduce-side guards in vlt-lint-full.js + homing malformed_frontmatter
goal: stop the full-lint reduce believing frontmatter_valid/frontmatter_issue on two claims it can check itself; give the class a definition, legal response and report slot
updated: 2026-08-26T09:57
---

- (direction) Edit run, not a new skill: target is skills/vlt-lint (references/checks.md, references/report.md) plus the workflow asset skills/vlt-setup/assets/workflows/vlt-lint-full.js. Brief factory/cycles/13-trusted-returns/briefs/build-1-reduce-side-guards.md is fully hardened; owner said build it, so the hardening beat is confirm-and-proceed.
- (decision) Memlog homed at factory/cycles/13-trusted-returns/build-1.memlog.md, NOT skills/vlt-lint/.memlog.md — .memlog.md is not gitignored and vlt-upgrade's own-the-apply is a filesystem copy of skills/, so a memlog under the shipped surface would ship into every vault (the exact hazard CLAUDE.md names for .decision-log.md).
- (note) Grounded at HEAD 668296b: collect() :542, unmarked_supersessions: collect(...) :576, malformed_frontmatter reduce :609, PAGE_SCAN.required :148 (14 names) — all four brief line refs confirmed exact.
- (decision) Disposition-2 authority resolved per the brief's binding: page-required frontmatter comes from {conventions}/frontmatter.md wiki-page schema = base (type, created, title, author, trust) + last_updated, summary, category, topic, status, sources. Documented-optional: review_after, source_type, review_note. PAGE_SCAN.required at :148 governs the AGENT return, never the PAGE, and is NOT the guard's authority.
- (gap) Brief F4 says put the report.md slot in the fix_now: block adjacent to frontmatter_drift (report.md:17). Source disagrees: the workflow emits malformed_frontmatter under flag_for_human (:609, between thin_pages and index_malformed). Documenting it in fix_now: would document a key the emitter never puts there and break the report schema's correspondence with the return. Resolving to flag_for_human after thin_pages (report.md:65) — deviation 1, to be numbered in the BUILT status.
- (decision) Guard shape: one shared residue rule under both dispositions. Normalize the claim (lowercase, punctuation->space), strip every recognized frontmatter key and bare connective filler; NON-EMPTY residue means the reduce cannot decide the claim, so no guard fires and the entry reports. This is the 'and NOTHING else' conjunction the brief demands, made mechanical.
- (decision) Refinement of disposition 2: implemented as 'every claimed-missing field is a DOCUMENTED-OPTIONAL page slot (review_after/source_type/review_note)' rather than the brief's literal 'claimed field not-in required-set'. The literal negation would refuse any missing-claim naming an unrecognized token — e.g. 'missing closing delimiter' — swallowing a genuine break, which is precisely the over-broad-guard risk acceptance check (2) exists to catch. The guard now only refuses what it can POSITIVELY identify. Numbered deviation 2.
- (event) F1 (:609) and F2 (:576 call site) applied. Verification 1/2/3 GREEN: harness over the shipped source with stubbed runtime and args as a JSON string. Six real 2026-08-25 returns + ashwagandha all REFUSED; three negative controls (genuine break, compound field-named, genuine supersession) plus a fourth (compound whose other half is free prose) all SURVIVE. Whole-return A/B against HEAD shows exactly two slots changed and every other key byte-equal, incl. collect()'s other consumers stale/contradictions. collect() at :542 byte-identical.
- (event) F3 (checks.md:15, Malformed frontmatter check entry w/ population, both exclusions + destinations, legal response) and F4 (report.md:66 slot) landed. R3 obligation discharged: the class had NO definition anywhere before this build.
- (decision) F4 deviation: the brief said fix_now: adjacent to frontmatter_drift (report.md:17). The emitter puts malformed_frontmatter in flag_for_human (between thin_pages and index_malformed), so the slot went to flag_for_human after thin_pages instead — documenting it in fix_now: would have documented a key the workflow never emits there. Numbered deviation 1.
- (event) Scrub caught a real leak: the disposition-2 code comment named the live vault page slug 'ashwagandha'. Rewritten to describe the confusion without the slug. Re-verified: none of the six slugs (nor ashwagandha) appears anywhere under skills/, .claude-plugin/, tools/ or CHANGELOG.md.
- (event) Release prep: both version strings 0.16.0 -> 0.16.1, CHANGELOG v0.16.1 entry dated 2026-08-26, branch cycle13-v0.16.1 cut. package-lint --expect-version 0.16.1 -> A/B/C/E PASS, D PASS. Tag/ff-merge/push HELD for the owner per the standing choreography.
- (decision) Bug found and fixed during review before commit: keys were scanned in declaration order, so 'type' shredded 'source_type' into a 'source' crumb and the optional key was unreachable. Replaced the two-pass claimResidue/fieldsNamed with a single parseClaim() that consumes keys LONGEST-FIRST and returns {named, residue}. 16/16 predicate cases now pass, including source_type.
- (event) Real-corpus probe (acceptance check 2) dispatched: 6 haiku readers over read-only copies of the six 2026-08-25 pages, driven by the workflow's OWN composed page-scan prompt (captured from the shipped source) against the live vlt-core conventions. Vault never written.
- (gap) Acceptance check (2), reported honestly: the six live probe scanners ALL returned frontmatter_valid: true with an empty frontmatter_issue and an empty unmarked_supersession — they did NOT reproduce the misroute. So zero of the six reach either class, which is what the check asks, but the guard was never exercised: the identical returns through the HEAD reduce give a byte-identical result. On its own that is exactly the non-probative at-rest pass the brief warns about.
- (event) Probative variant run to close that gap: the same six REAL pages carrying the returns the FIELD actually produced on 2026-08-25 (5x frontmatter_valid:false + 'missing verified_by/verified_at', 1x the same string in unmarked_supersession). PRE-GUARD 5 malformed_frontmatter + 1 unmarked_supersessions; POST-GUARD both EMPTY, with all six still in unattested_write under their real created dates and the census at 6/0/0/6. That is the guard doing the work on the real corpus.
- (note) Side observation, out of scope and already carried: the probe scanners paraphrased the verbatim summary field and HTML-escaped ampersands in category (&amp;) — Finding 4's blast radius, ruled OUT by Q1 and sitting in the roadmap's Carried-forward section. Not touched here.
- (event) Brief status rewritten to a BUILT record with per-F-site landings, verification as real numbers/text, acceptance evidence (incl. the honest check-2 caveat + the probative variant), and 5 numbered deliberate deviations. Roadmap carry 3 added and updated: stamped. Session complete pending commit.
