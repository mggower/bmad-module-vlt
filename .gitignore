# Build output
_output/
.DS_Store

# Python bytecode (merge scripts run via uv/python at install time)
__pycache__/
*.pyc

# Upstream BMad tooling (installed, not ours — defects file upstream) + local editor/session config
.claude/skills/bmad-*
.claude/settings.local.json
_bmad/
.vscode/

# Local-only: per-skill mint decision logs (internal dev records; some name the dev vault)
**/.decision-log.md
CLAUDE.local.md
