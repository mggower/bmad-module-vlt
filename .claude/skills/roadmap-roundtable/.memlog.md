---
skill: roadmap-roundtable
intent: build
updated: 2026-08-17T15:10
---

- (decision) Owner ruled (AskUserQuestion, 2026-08-17): required lifecycle stage; full installed BMad roster via roster discovery; standalone mechanics (no bmad-party-mode dependency); keepsake HTML always part of the contract.
- (decision) Home is .claude/skills/ beside lifecycle siblings, deliberately NOT bmad_builder_output_folder (skills/ = shipped module surface; a dev lifecycle skill there would ship into vaults).
- (decision) Name: roadmap-roundtable (from the Arc 7 keepsake's own vocabulary). Grounding source: _output/party-mode/2026-08-15-arc7-roadmap-roundtable.html — the worked example; skill teaches the method (hunt the joints; converge to amendments/rules/disputes; apply in-session), not that session's answers.
- (direction) Adoption wiring in scope: vlt-lifecycle.md (loop row + observable row + blocked route), build-brief readiness gate (review record or owner waiver), CLAUDE.md loop list. lifecycle-status needs no edit (derives from map).
- (decision) Review adopted as first-class lifecycle step 4 (full renumber: Brief 5, Build 6, Release 7, acceptance/closeout 8a-c) rather than a '3.5' patch. Edits: vlt-lifecycle.md (loop row, two observable rows incl. 'Review unresolved', blocked routes), build-brief (readiness-gate bullet + blocked causes + step refs), ideation-scaffold (next move now routes to the roundtable), vlt-release/acceptance-discharge/arc-closeout (step refs), CLAUDE.md (step 4 inserted, 8-step loop).
- (decision) No customize.toml — consistent with every lifecycle sibling; single-owner repo skill, nothing to override. Revisit only if a second operator appears.
- (event) Verification: quick_validate + scan-path-standards + scan-scripts all clean; SKILL.md 1700 tokens (under 2000 desired); stale-step-ref sweep clean; real-input dry-run vs open Arc 8 roadmap derives blocked:rulings-unfilled, matching lifecycle-status's known position.
- (note) Headless mode deliberately cannot rule disputes (substituted owner ruling = the defect B7-7 exists to prevent); it records OPEN disputes and routes to the owner. First live run expected at Arc 8 ideation review.
- (event) analyze: grade good, 0 critical / 1 high / 4 medium / 7 low, report .analysis/2026-08-17-1504/skill-analysis-report.html
- (event) fix pass applied ranks 1-3 of the 2026-08-17-1504 analyze report: session working file (_output/party-mode/<date>-arc<N>-roadmap-roundtable-session.md) between Hunt and Converge + headless audit clause (resolves enhancement-1, architecture-4); owner edges — roster steer/excuse at Convene, soft gate before Converge (enhancement-2/3/4); headless path names the table's merge-and-shape pass and undisputed-R# declaration-only posture (architecture-1). Ranks 4-5 deferred: polish until after the first live run; bare project paths are house style across siblings.
